---
registre: decisions
id_format: BDR-XXX
description: Journal des décisions structurantes prises sur le projet thenaisiepierre.fr — quoi, pourquoi, contre quoi.
champs:
  - id: identifiant unique BDR-XXX (incrémental, jamais réutilisé)
  - date: AAAA-MM-JJ
  - titre: intitulé court de la décision
  - decision: ce qui a été choisi, formulé sans ambiguïté
  - pourquoi: la raison réelle (contrainte, retour utilisateur, incident)
  - alternatives: ce qui a été envisagé et écarté, et pourquoi
  - statut: actif | révisé | abandonné
---

# Décisions — thenaisiepierre.fr

Vitrine personnelle de Pierre : sa vie, son parcours, et les sujets qui l'intéressent (RCH, portfolio technique, galerie).

## Index

| ID | Date | Titre | Statut |
|---|---|---|---|
| BDR-001 | 2026-07-20 | Aucune barre de navigation globale | actif |
| BDR-002 | 2026-07-21 | Shaders WebGL maison plutôt que composants React/Magic MCP | actif |
| BDR-003 | 2026-07-22 | Rester sur Pelican + Anime.js auto-hébergé, pas de migration | actif |
| BDR-004 | 2026-07-22 | Fonds « ethereal » (filtre SVG) au lieu des shaders WebGL warp.js/mesh.js | actif |
| BDR-005 | 2026-07-22 | Ethereal piloté par GSAP, palette noir/blanc + touche de vert, un seul calque animé | révisé |
| BDR-006 | 2026-07-22 | Ethereal : rythme proche de l'origine, palette vert/beige, fond vert forêt (fin du noir) | révisé |
| BDR-007 | 2026-07-22 | Vert du shader ancré sur `--vert` (pied de page), éclairci à la même teinte/saturation | révisé |
| BDR-008 | 2026-07-22 | Shader 100% vert (fin du beige/blanc) : calque mono en `--vert-clair`, accent en vert éclairci | actif |
| BDR-009 | 2026-07-22 | Accueil éclairci (fond `--vert`, voile allégé) ; cartes du sommaire repassées en beige | actif |
| BDR-010 | 2026-07-22 | Règle « beige sur vert » sur toute la page d'accueil ; cadre des cartes rendu invisible | révisé |
| BDR-011 | 2026-07-22 | Beige de l'accueil assombri (`--sable-fonce`) ; bande du séparateur de section rendue invisible | révisé |
| BDR-012 | 2026-07-22 | `--sable-fonce` réclairci, nuances sombres en vert forêt (jamais noir) | révisé |
| BDR-013 | 2026-07-22 | Bloc qui tourne : hero ↔ sommaire liés par un flip 3D épinglé (GSAP ScrollTrigger) | abandonné |
| BDR-014 | 2026-07-22 | Retour à l'empilement simple accueil ; suppression de l'apparition au scroll des cartes du sommaire | actif |
| BDR-015 | 2026-07-22 | `--sable-fonce` reclairci vers le blanc, repris pour les titres de toutes les pages ; barres latérales des callouts RCH retirées | révisé |
| BDR-016 | 2026-07-22 | `--sable-fonce` fixé à la couleur exacte demandée `#ffefcb` | actif |
| BDR-017 | 2026-07-24 | Suppression des pages Photo (fantôme), Portfolio et À propos ; sommaire de l'accueil ramené à 3 cartes égales | actif |
| BDR-018 | 2026-07-24 | Polices Google Fonts auto-hébergées (sous-ensemble latin), logos CV externes remplacés par des monogrammes CSS | actif |
| BDR-019 | 2026-07-24 | Galerie alimentée par un lien de partage Immich, récupéré au moment du build (pas côté navigateur) | révisé |
| BDR-021 | 2026-07-24 | Script `build_gallery.py` : identifiant slug/key testé dans l'ordre, User-Agent de navigateur pour passer le WAF Cloudflare | actif |
| BDR-020 | 2026-07-24 | Nouvelle variable `SITE_ABSOLUTE_URL`, distincte de `SITEURL`, pour les balises exigeant une URL absolue (canonical, Open Graph) | actif |
| BDR-023 | 2026-07-24 | Contraste du titre d'accueil corrigé (`--vert-pale`) ; `.nav-logo`/`.footer-logo` transformés en liens vers l'accueil | actif |
| BDR-024 | 2026-07-24 | Retrait complet de GSAP et Anime.js (169 Ko vendor) ; effets reconvertis en CSS/vanilla déjà présents dans le repo | actif |
| BDR-025 | 2026-07-24 | Typographie Fraunces/Inter remplacée par Petrona/Karla, auto-hébergées (poids typo −65 %) | actif |
| BDR-026 | 2026-07-24 | Sommaire (TOC) ajouté sur `cv.html`, sur le modèle de `maladie.html` | actif |
| BDR-027 | 2026-07-24 | Compteurs animés retirés (page RCH), chiffres épidémiologiques rendus statiques | révisé |
| BDR-028 | 2026-07-24 | Densité des tirets cadratins réduite dans `cv.md`/`maladie.md` (prose seulement, dates/lieux/citations inchangés) | actif |
| BDR-029 | 2026-07-24 | Vrais logos entreprises/écoles restaurés sur le CV (revient sur une partie de BDR-018), auto-hébergés | actif |
| BDR-030 | 2026-07-24 | Refonte de l'accueil : hero décluttéré (shader → dégradé CSS), sommaire en index éditorial, geste de scroll signature en CSS natif | actif |
| BDR-031 | 2026-07-24 | Casse-cache `?v=<sha-git>` sur CSS/JS après un déploiement resté visuellement invisible (cache navigateur) | actif |
| BDR-032 | 2026-07-24 | Retrait des pictogrammes décoratifs et des dégradés « lavis » sur toutes les pages internes (`_deco.html` supprimé) | actif |
| BDR-033 | 2026-07-24 | `.footer-logo` renvoie vers un mailto plutôt que l'accueil ; `.nav-logo` redevient un texte inerte | révisé (annule une partie de BDR-023) |
| BDR-034 | 2026-07-24 | Tracé des schémas RCH réécrit en CSS pur (`pathLength`), dégradation sans JS = tracé déjà visible ; texte de description du 1er schéma élargi/scindé (débordement corrigé) | révisé |
| BDR-036 | 2026-07-24 | Tracé des schémas RCH repassé en JS impératif (`requestAnimationFrame`), toujours avec `pathLength`, après échec confirmé de la version CSS pure | actif |
| BDR-037 | 2026-07-24 | Compteurs animés restaurés (annule BDR-027), même technique rAF que le tracé des schémas | actif |
| BDR-038 | 2026-07-24 | URLs propres : `PAGE_URL`/`PAGE_SAVE_AS` en `{slug}/` plutôt que `pages/{slug}.html`, tous les liens internes mis à jour | actif |
| BDR-039 | 2026-07-24 | Trace des schémas confirmé fonctionnel (BDR-036) ; halo de fond ajouté aux étiquettes SVG pour rester lisibles au-dessus du tracé | actif |
| BDR-035 | 2026-07-24 | Accueil : bande blanche entre sommaire et pied de page supprimée ; étiquette « sommaire » retirée | actif |

## Entrées

### BDR-001 — Aucune barre de navigation globale

**Date :** 2026-07-20
**Décision :** Le site n'a pas de menu listant toutes les pages. Chaque page (Parcours, Portfolio, Vivre avec la RCH, Galerie) est volontairement cloisonnée.
**Pourquoi :** Pierre partage des liens différents à des publics différents (ex. un recruteur ne reçoit pas forcément le lien vers la page santé). Il ne veut pas qu'un visiteur découvre facilement les autres sections en naviguant depuis une seule.
**Alternatives envisagées :** Menu classique avec tous les liens en en-tête — écarté car ça va directement à l'encontre de l'objectif de cloisonnement.
**Statut :** actif. Ne pas réintroduire de nav globale sans revalider avec Pierre.

### BDR-002 — Shaders WebGL maison plutôt que composants React

**Date :** 2026-07-21
**Décision :** Les effets visuels demandés depuis 21st.dev (wavy background, wrap-shader, MeshGradient, Velaris) sont systématiquement réimplémentés en GLSL/JS vanilla (`theme/js/warp.js`, `theme/js/mesh.js`) plutôt qu'installés via `npx shadcn add`.
**Pourquoi :** Le site est du Pelican statique (pas de React, Tailwind, TypeScript, ni build). Les composants `@paper-design/shaders-react` sont inutilisables tels quels. Le MCP `@21st-dev/magic` est de plus cassé en amont (voir BLK-001).
**Alternatives envisagées :** Migrer tout le site vers Next.js/React pour profiter des composants du marché — écarté, disproportionné par rapport au besoin (site vitrine statique).
**Statut :** actif. Tout nouveau composant 21st.dev demandé doit suivre ce même chemin : identifier le principe visuel (bruit simplex, mesh gradient…) et le recoder en vanilla.

### BDR-003 — Rester sur Pelican + Anime.js auto-hébergé

**Date :** 2026-07-22
**Décision :** Pour intégrer Anime.js, le site reste sur Pelican. La bibliothèque (UMD minifiée, v4.5.0) est auto-hébergée dans `theme/js/vendor/anime.min.js` plutôt que chargée depuis un CDN.
**Pourquoi :** Pierre avait explicitement autorisé à sortir de Pelican « si ça permet de mieux respecter la taille sur le disque OVH ». Vérifié : Pelican est un outil de build, rien de son installation n'est déployé sur OVH (seul `output/` part en FTP) — donc aucun argument de taille ne justifie une migration. Anime.js pèse 116 Ko (≈40 Ko gzippé), l'intégralité du site généré ne pèse que 300 Ko : négligeable sur un hébergement mutualisé.
**Alternatives envisagées :** Charger Anime.js depuis jsDelivr/unpkg — écarté pour rester cohérent avec la philosophie déjà actée (BDR-002) de ne dépendre d'aucun service tiers au runtime.
**Statut :** actif. Ne pas reproposer de migration hors Pelican pour des raisons de taille — l'argument ne tient pas pour ce projet.

### BDR-004 — Fonds « ethereal » (filtre SVG) au lieu des shaders WebGL

**Date :** 2026-07-22
**Décision :** `warp.js` et `mesh.js` (canevas WebGL, cf. BDR-002) sont supprimés et remplacés partout par `theme/js/ethereal.js` — un fond animé en filtre SVG natif (`feTurbulence` + `feColorMatrix type="hueRotate"` animé), réimplémentation vanilla du composant « Etheral Shadow » de 21st.dev (React + `framer-motion`, écarté pour les mêmes raisons que BDR-002).
**Pourquoi :** Pierre trouvait les shaders WebGL pixellisés, avec un mouvement peu visible et des couleurs trop sombres. Cause racine du pixel : `mesh.js` rendait sur un canevas sous-échantillonné à 45 % de la résolution réelle pour limiter le coût CPU/GPU. Un filtre SVG est rendu nativement à pleine résolution par le moteur du navigateur — plus jamais de sous-échantillonnage.
**Alternatives envisagées :** Augmenter la résolution interne du canevas WebGL existant — écarté, le coût de performance aurait été bien plus élevé qu'un filtre SVG natif pour un résultat toujours en dents de scie sur petits écrans/zoom.
**Statut :** actif. Tout nouveau bandeau/fond animé doit utiliser `.ethereal` (voir LRN-004 pour le calibrage empirique du contraste couleur/opacité et LRN-005 pour la marge de voile texte).

### BDR-005 — Ethereal piloté par GSAP, palette noir/blanc + touche de vert, un seul calque animé

**Date :** 2026-07-22
**Décision :** `ethereal.js` anime désormais la teinte via `gsap.to()` (au lieu d'une boucle `requestAnimationFrame` maison), avec `gsap.ticker.lagSmoothing(0)` désactivé globalement. Chaque zone a deux calques `.ethereal` : un calque **mono** (gris clair/blanc, 205-235/255) qui seul est animé, et un calque `.ethereal--accent` (vert, fusionné en `mix-blend-mode: screen`) **figé** sur un angle de teinte fixe dérivé du seed — il ne bouge pas. Fonds/voiles passés au noir neutre `#0a0a0a`/`rgba(5-6,5-6,5-6,…)` (à la place du vert foncé `#0d2415`).
**Pourquoi :** Pierre demandait (1) un mouvement bien plus rapide et marqué, en utilisant GSAP explicitement plutôt que la boucle maison, et (2) moins de noir dominant, plutôt un style noir/blanc classe avec des touches de vert. Mesure réelle : avec les 2 calques par zone animés simultanément (4 filtres SVG recalculés à chaque frame), le fps tombait à ~25, ce qui déclenchait le lag smoothing de GSAP et ralentissait la vitesse perçue d'un facteur ~3 par rapport à la config (voir LRN-006). Geler le calque accent (pas besoin de bouger pour être visible) divise par ~2 le coût de rendu par zone et laisse le calque mono tourner à la vitesse réellement configurée.
**Alternatives envisagées :** Garder les deux calques animés mais réduire `numOctaves`/la zone de filtre pour limiter le coût — écarté, plus simple et plus sûr de ne pas animer ce qui n'a pas besoin de l'être. Mesurer la vitesse perçue en conditions réelles via l'outil de prévisualisation — écarté, l'onglet ne passe jamais au premier plan dans cet outil (voir LRN-007), rendant toute mesure de temps réel invalide ; vérification faite sur la config GSAP (`tween.duration()`) et sur le contraste/la teinte rendus (rasterisation canvas), pas sur un chronométrage à l'écran.
**Statut :** révisé par BDR-006 le même jour (vitesse jugée trop marquée une fois le vrai fps rétabli, et palette repassée vert/beige à la demande de Pierre). L'architecture (GSAP, `lagSmoothing(0)`, un seul calque animé par zone) reste valable et inchangée — seuls la vitesse et les couleurs ont été retouchées.

### BDR-006 — Ethereal : rythme proche de l'origine, palette vert/beige, fin du noir

**Date :** 2026-07-22
**Décision :** Vitesse : `durationSec = mapRange(speed, 1, 100, 22, 2.2)` (au lieu de `3.2, 0.45`) — un cycle dure maintenant ~4,2s à vitesse 90, contre ~5s pour le tout premier réglage (`26, 2.6`) : environ 15 % plus rapide que l'origine, pas un mouvement effréné. Couleurs : le calque mono passe du blanc/gris à un beige chaud (`--sable-2`, 239,228,213) et le calque accent reste vert (`--vert-clair`, 91,140,62). Tous les fonds/voiles (`.shader-hero`, `.hero-immersif`, `.bande-abstraite`, `.shader-scrim`, `.mesh-veil`, `.home .hub-card`) passent du noir neutre (`#0a0a0a` / rgba(5-6,…)) à un vert forêt très sombre (`#111f16` / rgba(11,20,14,…)) — il n'y a plus aucune valeur de gris/noir neutre dans tout le système de fond ethereal.
**Pourquoi :** Pierre trouvait le rythme (fixé la veille sur `3.2, 0.45`) trop rapide une fois le vrai fps rétabli (voir BDR-005/LRN-006) — il demandait de revenir au rythme de base, en un peu plus rapide seulement. Il demandait aussi de sortir du noir/blanc et d'aller vers un mélange vert/beige, sans aucun noir dans le shader.
**Alternatives envisagées :** Basculer toute la section hero en mode clair (fond beige clair, texte sombre) pour un rendu plus littéralement « beige » — écarté : Pierre a dit « enlever le noir dedans » (dans le shader), pas de refaire toute la charte des héros ; un fond vert forêt très sombre avec un shader beige/vert satisfait la demande sans réécrire toutes les règles de texte blanc du site (risque de casser des lisibilités ailleurs pour un gain non demandé).
**Statut :** révisé par BDR-007 le même jour (couleur du vert accent retouchée, tout le reste — vitesse, fonds, architecture — inchangé). Contraste re-vérifié après ce changement : page-hero (cv/maladie) pire cas 6,0-7,3:1 sur 6 angles à 1440px (texte à 91 % de largeur) ; hero-immersif (accueil), après resserrage du `.mesh-veil` (opacités +0,06 à +0,08), pire cas 5,71:1 partout (contre 4,49:1 avant resserrage, trop proche du seuil AA 4,5:1). Vert et beige confirmés tous deux présents dans le rendu (matrices de sortie vérifiées : 239,228,213 et 91,140,62). Aucune trace de noir dans les fonds calculés (`getComputedStyle` sur les 3 pages : toujours `rgb(17,31,22)`).

### BDR-007 — Vert du shader ancré sur `--vert` (pied de page), éclairci à la même teinte/saturation

**Date :** 2026-07-22
**Décision :** Le calque accent du shader passe de `91,140,62` (proche de `--vert-clair`) à `150,195,155` — calculé en conservant exactement la teinte (127°) et la saturation (27 %) de `--vert` (#2f5233, le vert du bloc de pied de page où figure le nom de Pierre), en augmentant seulement la luminosité (25 % → 68 % en HSL). Le calque mono (beige, `--sable-2`) et tout le reste (vitesse, fonds vert forêt) restent inchangés (BDR-006).
**Pourquoi :** Pierre a désigné explicitement le vert du pied de page (« ce vert avec des nuances plus claires encore ») comme référence, plutôt que le vert du shader existant — il voulait un vert bien plus clair mais dans cette même famille précise, pas une teinte différente.
**Alternatives envisagées :** Éclaircir par simple mélange linéaire vers le blanc (`base + (255-base)*t`) — écarté après calcul : à t=0.6 ça donne `172,186,173`, presque gris (la saturation chute trop vite en interpolation linéaire RGB). Conserver teinte/saturation en HSL et ne monter que la luminosité donne un vert clair qui reste identifiable comme « le même vert », conformément à la demande.
**Statut :** révisé par BDR-008 le même jour (calque mono repassé au vert, le beige disparaît complètement). Contraste revérifié après le changement (le calque accent, plus lumineux, brille davantage en fusion « screen ») : page-hero pire cas 5,85-7,15:1 (légèrement au-dessus de l'ancien 6,0-7,3 sur certains angles, toujours largement au-dessus du seuil AA) ; accueil pire cas 5,61:1 (contre 5,71:1 avant, marge quasi identique). Aucune erreur console, aucun débordement sur les 3 pages testées.

### BDR-008 — Shader 100% vert, harmonisé avec la palette du site

**Date :** 2026-07-22
**Décision :** Le calque mono (celui qui bouge) repasse du beige (`--sable-2`, 239,228,213) au vert moyen `--vert-clair` (91,140,62). Le calque accent garde le vert clair de BDR-007 (150,195,155, dérivé de `--vert`). Le fond reste le vert forêt très sombre de BDR-006 (`#111f16`). Le shader est donc désormais composé exclusivement de trois verts déjà présents dans la charte du site (`--vert` en filigrane via le fond, `--vert-clair` en calque principal, une variante éclaircie de `--vert` en accent) — plus aucun beige ni blanc.
**Pourquoi :** Pierre a demandé de ne quasiment plus avoir de blanc dans le shader et de revenir à un vert complet, avec de l'harmonie entre les pages et les bannières. Réutiliser les teintes déjà définies dans `:root` (plutôt que d'en inventer de nouvelles) garantit cette harmonie par construction : le shader ne fait plus qu'exprimer la palette déjà en place ailleurs sur le site (bouton, liens, pied de page).
**Alternatives envisagées :** Inventer une nouvelle teinte de vert dédiée au shader — écarté, va à l'encontre de la demande d'harmonie ; réutiliser les variables existantes est plus cohérent et plus simple à maintenir (un seul jeu de verts pour tout le site).
**Statut :** actif. Contraste nettement amélioré par ce changement (le vert moyen est plus sombre que l'ancien beige) : accueil pire cas 7,93:1 (contre 5,61:1 avant), page-hero pire cas 8,58-9,51:1 (contre 5,85-7,15:1 avant) — au-dessus du seuil AAA sur toutes les pages et tous les angles testés. Cohérence vérifiée : les mêmes deux couleurs vertes (91,140,62 / 150,195,155) apparaissent sur les 3 pages testées (accueil, cv, maladie), aucune erreur console, aucun débordement.

### BDR-009 — Accueil éclairci, cartes du sommaire repassées en beige

**Date :** 2026-07-22
**Décision :** `.hero-immersif` passe du quasi-noir vert (`#111f16`) au vert plein `var(--vert)` (#2f5233) — exactement la couleur du pied de page (`.footer`), qui utilise déjà du texte blanc/pâle dessus avec succès. Le `.mesh-veil` est très allégé (opacités radiales/linéaires environ divisées par 2-2,5, ex. le point le plus sombre passe de 0,78 à 0,38). Les cartes du sommaire (`.home .hub-card`, les 4 tuiles Parcours/RCH/Portfolio/Galerie) redeviennent des panneaux beiges (`rgba(247,241,232,.88)`, `--sable`) avec accents marron (`--marron-fonce`, `--marron`) au lieu du vert menthe sur fond sombre — texte interne repassé en sombre (`--encre`/`--mut`) puisque le panneau est maintenant clair. Périmètre limité à la page d'accueil : `.shader-hero`/`.bande-abstraite` (CV, RCH) ne sont pas concernés, seule la page d'accueil avait été jugée trop sombre.
**Pourquoi :** Pierre trouvait la page d'accueil encore trop sombre et a demandé de s'inspirer de ce qui se fait sur d'autres sites, en autorisant explicitement à moins se soucier du contraste. Il a aussi demandé que les cartes-liens du sommaire (« les sortes de boutons ») redeviennent beige, sans changer leur comportement (liens, survol, glow qui suit la souris via `--mx`/`--my` — tout ce JS reste intact, seules les couleurs CSS changent).
**Alternatives envisagées :** Continuer à assombrir via un voile dense (l'approche des BDR précédents) — écarté, c'est exactement ce que Pierre trouvait trop sombre ; réutiliser `--vert` (déjà éprouvé via le pied de page) plutôt qu'inventer une nouvelle teinte plus claire, pour rester cohérent avec BDR-008 (harmonie via les variables existantes).
**Statut :** actif. Contraste non re-vérifié par rasterisation complète (Pierre a explicitement demandé de ne pas s'en inquiéter) — vérification légère seulement : blanc sur `--vert` plein calcule à 8,84:1 (bien au-dessus des seuils usuels, avant même l'apport du voile), et le duo beige/`--encre`+`--mut` des cartes est le même déjà utilisé ailleurs sur le site (ex. `.toc`) donc déjà éprouvé. Aucune erreur console, aucun débordement.

### BDR-010 — Règle « beige sur vert » généralisée à l'accueil, cadre des cartes invisible

**Date :** 2026-07-22
**Décision :** Tout texte reposant directement sur le fond vert de l'accueil passe en beige (`var(--sable)` / `var(--sable-2)`) : `.hero-label`, `h1`, `.hero-sub`, `.btn-verre` (texte + fond + bordure), `.section-label` et ses pseudo-éléments, `.picto`/`.picto-plein`, les lueurs de `.deco-layer`, `.hero-scroll`/`.hero-scroll-line`. Seule exception, en nuance : le mot en italique dans le `h1` (`em`) passe en `var(--encre)` (sombre) plutôt qu'en beige, pour une touche gravée/en creux. Par ailleurs, la bordure `2px solid` des cartes du sommaire (`.home .hub-card`, héritée de la règle de base `.hub-card`) passe en `border-color: transparent` (au lieu de `border: none`, pour ne pas décaler le padding) — le fond beige et l'ombre suffisent à distinguer la carte, sans le cadre qui la faisait ressembler à une page à part entière.
**Pourquoi :** Pierre a demandé que tout ce qui repose sur du vert (notamment les polices) passe en beige, avec une tolérance explicite pour des nuances en vert foncé/noir ; et que le cadre visible autour des cartes-boutons du sommaire soit rendu invisible, car il donnait l'impression d'« une page dans la page ».
**Alternatives envisagées :** Utiliser du vert foncé/noir pour la nuance du `em` ET pour d'autres éléments (ex. le label) — écarté après calcul de contraste (`--encre` sur `--vert` ≈ 1,77:1, déjà très sourd) : généraliser le noir/vert foncé à plus d'un élément aurait rendu plusieurs textes illisibles plutôt qu'un seul, plus discret par conception. Retirer complètement la bordure (`border: none`) plutôt que la rendre transparente — écarté, aurait décalé la taille de la carte de 4px (box-sizing border-box).
**Statut :** révisé par BDR-011 le même jour (Pierre a clarifié que le vrai « cadre » qui le gênait était ailleurs — voir ci-dessous — et a aussi demandé un beige plus soutenu). Vérifié à l'époque : `h1`/`label`/`sub`/bouton en beige (`rgb(247,241,232)`), `em` en sombre (`rgb(35,36,31)`), bordure des cartes à `rgba(0,0,0,0)` (transparente, largeur 2px inchangée — pas de saut de mise en page). Contraste beige/vert mesuré à 7,87:1 (large marge) ; contraste `em`/vert mesuré à 1,77:1 — assumé et attendu (nuance volontairement discrète, Pierre a demandé de ne pas s'inquiéter du contraste), signalé explicitement plutôt que masqué. Aucune erreur console, aucun débordement.

### BDR-011 — Beige de l'accueil assombri, bande du séparateur de section rendue invisible

**Date :** 2026-07-22
**Décision :** Nouvelle variable `--sable-fonce: #d8c3a0` (beige plus soutenu, entre `--sable-2` et `--marron`), utilisée à la place de `--sable`/`--sable-2` pour tout le beige de l'accueil (texte du hero, bouton, label de section, pictogrammes, lueurs, indicateur de défilement, fond des cartes du sommaire). Le vrai « cadre » que Pierre voulait invisible était en fait `.home .section-label::after` — le trait de séparation quasi pleine largeur après le label « // sommaire », qui lisait comme une bande translucide autour du sommaire — passé en `background: transparent` (BDR-010 avait à tort traité la bordure des cartes comme LE cadre concerné ; cette bordure reste transparente, ce qui était de toute façon une amélioration correcte, mais ce n'était pas ce qu'il visait). Effet de bord corrigé au passage : `--mut` (conçu pour `--sable` très pâle) tombait à 2,97:1 sur le nouveau beige plus sombre des cartes — remonté à 6,19:1 avec un ton dédié (`#3f3f36`) réservé à `.home .hub-card p`.
**Pourquoi :** Pierre a demandé de continuer l'effort avec un beige plus foncé, et a clarifié qu'il parlait en réalité de la bande transparente autour du sommaire (pas du cadre des cartes) qu'il ne voulait pas voir.
**Alternatives envisagées :** Modifier `--sable`/`--sable-2` directement (variables globales) — écarté, elles servent aussi ailleurs sur le site (ex. `.toc`) et les changer aurait eu un effet de bord hors du périmètre demandé (« cette page d'accueil »). Une nouvelle variable dédiée à l'accueil garde le changement scopé et documenté.
**Statut :** révisé par BDR-012 le même jour (Pierre voulait ce beige un cran plus clair, et le noir/gris utilisé pour les nuances remplacé par du vert forêt). Vérifié à l'époque : `h1`/fond des cartes en `rgb(216,195,160)`, séparateur de section à `rgba(0,0,0,0)` (invisible). Contraste beige foncé/vert : 5,15:1 (toujours largement au-dessus AA malgré l'assombrissement) ; `--encre` sur beige foncé : 9,10:1 ; texte des cartes (`#3f3f36`) sur beige foncé : 6,19:1 (corrigé depuis 2,97:1). Aucune erreur console, aucun débordement.

### BDR-012 — `--sable-fonce` réclairci, nuances sombres en vert forêt (jamais noir)

**Date :** 2026-07-22
**Décision :** `--sable-fonce` passe de `#d8c3a0` à `#e6d2b0` (un cran plus clair). Les couleurs sombres utilisées pour le contraste sur fond beige/vert ne sont plus neutres (`--encre` #23241f, ou le gris `#3f3f36`) mais du vert forêt : `.home .hub-card h3` et `.home .hub-card p` passent tous les deux à `var(--vert)` (5,98:1 sur le nouveau beige) ; le mot en italique du `h1` (nuance sombre sur fond vert) passe à un vert forêt encore plus sombre que le fond (`#17241a`, distinct de `var(--vert)` pour rester visible — l'utiliser tel quel aurait rendu le mot invisible, texte et fond étant alors la même couleur exacte).
**Pourquoi :** Pierre a demandé un beige un peu plus clair, et de ne plus utiliser de noir pour les contrastes de texte, mais du vert forêt à la place.
**Alternatives envisagées :** Utiliser `var(--vert)` tel quel pour le mot en italique du titre (qui repose sur un fond `var(--vert)` identique) — rejeté immédiatement, contraste 1:1, le mot aurait disparu ; une teinte volontairement plus sombre que le fond était nécessaire pour qu'il reste visible tout en étant « vert forêt » comme demandé.
**Statut :** révisé par BDR-015 le même jour (Pierre a demandé un cran de clair supplémentaire ; au passage, découvert que plusieurs usages `rgba()` littéraux — hero-sub, bouton, picto, lueurs, indicateur de défilement — étaient restés sur l'ANCIENNE valeur `216,195,160` faute d'avoir été mis à jour ici, corrigé en même temps). Vérifié à l'époque : `h1` et fond des cartes en `rgb(230,210,176)` ; `em` en `rgb(23,36,26)` (contraste 1,82:1 sur le hero — nuance volontairement discrète, cohérente avec BDR-010) ; `h3`/`p` des cartes en `rgb(47,82,51)` (contraste 5,98:1 sur le nouveau beige). Aucune erreur console, aucun débordement.

### BDR-013 — Bloc qui tourne : hero ↔ sommaire liés par un flip 3D épinglé

**Date :** 2026-07-22
**Décision :** GSAP ScrollTrigger (plugin officiel, auto-hébergé dans `theme/js/vendor/ScrollTrigger.min.js`, même version 3.15.0 que `gsap.min.js`) épingle un nouveau conteneur `.flip-stage` (qui enveloppe `.hero-immersif` et `#sommaire`, désormais tous deux `.flip-face`) pendant un défilement de `+=100%` (une hauteur d'écran), et fait pivoter en 3D le contenu du hero (`rotateX` 0→-70°, fondu vers 0) pendant que le sommaire pivote en sens inverse pour apparaître (`rotateX` 85°→0°, fondu vers 1) — au même endroit à l'écran, d'où l'effet de bloc qui se retourne sur lui-même. Nouveau fichier `theme/js/flip-transition.js`. Activé uniquement à partir de 900px (`gsap.matchMedia`) et jamais si `prefers-reduced-motion: reduce` ; en dessous, ou sans JS/GSAP, `.flip-stage` reste un conteneur neutre et les deux sections s'empilent normalement (dégradation totale, zéro dépendance au JS pour lire la page).
**Pourquoi :** Pierre voulait un vrai effet de défilement entre le titre et le sommaire de l'accueil, « comme si on tournait un bloc », en s'inspirant d'une animation adaptée d'esprit 21st.dev (toujours réimplémentée en vanilla, jamais installée telle quelle — voir BDR-002).
**Alternatives envisagées :** Laisser `#sommaire` entrer par défilement naturel après l'épinglage du hero (deux ScrollTrigger séparés, sans conteneur partagé) — écarté après analyse : avec l'espacement de pin par défaut, le sommaire resterait physiquement hors écran (poussé par le spacer) pendant toute la durée du scrub, donc son animation d'entrée se terminerait AVANT qu'il ne devienne visible par le défilement naturel — aucune des deux faces ne serait visible simultanément, l'effet « bloc qui tourne » ne se produirait jamais à l'écran. D'où le choix d'empiler les deux faces dans la même cellule de grille (`.flip-face { grid-area: 1/1 }`) pour qu'elles occupent réellement le même rectangle pendant la transition.
**Statut :** abandonné par BDR-014, le jour même. L'effet fonctionnait techniquement (voir ci-dessous les vérifications faites), mais Pierre l'a trouvé finalement pas voulu — il cherchait un défilement « lisse », pas un bloc qui pivote. **Bug trouvé et corrigé en vérifiant, avant l'abandon** : `html { scroll-behavior: smooth }` (réglage global existant, pour les ancres comme `#sommaire`) entre en conflit avec ScrollTrigger — confirmé documenté par GSAP lui-même — et provoquait un `progress` d'épinglage figé à ~0 malgré un scroll réel (voir LRN-008). Deuxième point de vigilance résolu par construction : `.mesh-holder` (fond ethereal `position:fixed`) a été sorti de `.flip-stage` pour rester un frère plutôt qu'un descendant, car la `perspective` posée sur `.flip-stage` aurait changé son bloc de positionnement (voir LRN-009). Vérifié en conditions réelles (scroll programmatique + `ScrollTrigger.update()`) : progress 0→0,5→1 correspondait à une vraie interpolation d'opacité et de matrice 3D sur les deux faces ; épinglage puis relâchement corrects ; repli mobile propre. Ces deux apprentissages (LRN-008, LRN-009) restent valables pour toute future tentative d'animation de scroll sur ce projet, même si cette implémentation précise a été retirée.

### BDR-014 — Retour à l'empilement simple de l'accueil, fin de l'apparition au scroll des cartes

**Date :** 2026-07-22
**Décision :** Le flip 3D hero/sommaire (BDR-013) est intégralement retiré : `.flip-stage`/`.flip-face` supprimés du gabarit et du CSS, `theme/js/flip-transition.js` et `theme/js/vendor/ScrollTrigger.min.js` supprimés du thème et de `output/`, script ScrollTrigger retiré de `base.html`. `.mesh-holder` reprend sa place d'origine, descendant de `.hero-immersif` (plus besoin d'en faire un frère puisqu'il n'y a plus de `perspective` sur un ancêtre). `scroll-behavior: smooth` restauré sur `html` (le conflit avec ScrollTrigger ne s'applique plus, cause disparue avec le plugin). Par ailleurs, `.hub-card` est retiré de la liste des éléments à révéler au scroll dans `base.html` (`.reveal`/IntersectionObserver) : les cartes du sommaire sont désormais toujours à `opacity:1`, sans aucune apparition/fondu au défilement.
**Pourquoi :** Pierre a abandonné l'effet de bloc qui tourne — il voulait un défilement lisse sur l'accueil, sans qu'aucune section « apparaisse » pendant le scroll. Le flip 3D allait à l'encontre de cette demande, et l'animation de révélation des cartes du sommaire (fondu + décalage vertical à l'entrée dans le viewport) en est une autre forme, retirée pour la même raison.
**Alternatives envisagées :** Garder ScrollTrigger installé mais désactivé (au cas où) — écarté, du code mort sans utilité connue alourdit inutilement le site pour rien (contraire à la philosophie d'empreinte minimale, BDR-003) ; plus simple de le retélécharger si un futur besoin scroll-driven se présente. Limiter le retrait de `.reveal` à une exception CSS plutôt que modifier la liste JS dans `base.html` — écarté, la liste JS est la source de vérité unique (elle seule décide qui reçoit `.reveal`), la modifier directement évite un CSS de contournement redondant.
**Statut :** actif. Vérifié : `window.ScrollTrigger` et `#flip-stage` absents du DOM/JS ; `.mesh-holder` de nouveau `position:fixed` avec `getBoundingClientRect()` à `{top:0,left:0}` ; shader intact (1 calque animé + 1 figé) ; les 4 cartes du sommaire à `opacity:1` sans classe `.reveal` ; `scroll-behavior:smooth` restauré. Vérifié sur le portfolio que `.project-card` garde bien son `.reveal` (9 cartes) — le retrait est bien scopé aux cartes de l'accueil, pas une régression globale. Aucune erreur console, aucun débordement.

### BDR-015 — `--sable-fonce` reclairci vers le blanc, repris pour les titres de toutes les pages, barres des callouts RCH retirées

**Date :** 2026-07-22
**Décision :** `--sable-fonce` passe de `#e6d2b0` à `#f4e0be`, plus proche du blanc. Tous les usages `rgba()` littéraux qui dupliquaient cette teinte (hero-sub, bouton `.btn-verre`, `.picto`, lueurs `.deco-layer`, indicateur `.hero-scroll`, fond `.hub-card`) mis à jour vers la nouvelle valeur — certains étaient restés bloqués sur l'ANCIENNE valeur `216,195,160` de BDR-011, jamais suivie lors du passage à BDR-012 (voir LRN-010). Cette même couleur est reprise pour `.page-hero .page-header h1` (titres des pages CV, RCH, portfolio, galerie), remplaçant le blanc pur — pour une cohérence de teinte entre l'accueil et le reste du site. Par ailleurs, la barre verticale de gauche des blocs `.callout` (`::before`, dégradé `--vert-clair` → `--marron`) est retirée (`display:none`) — utilisée sur la page RCH pour les 5 blocs de contexte/explication (« À lire avant tout », « Le signe à ne pas minimiser », etc.).
**Pourquoi :** Pierre voulait les boutons/titres de l'accueil encore plus clairs, proches du blanc, et a demandé de reprendre cette couleur pour les titres de toutes les autres pages. Séparément, il voulait retirer les barres latérales des blocs de texte explicatif/contexte sur la page RCH, qu'il ne trouvait pas nécessaires.
**Alternatives envisagées :** Garder le blanc pur (`#ffffff`) pour les titres des autres pages et ne changer que l'accueil — écarté, Pierre a explicitement demandé de reprendre LA MÊME nouvelle couleur partout, pas de garder deux traitements différents. Masquer la barre des callouts via une classe CSS additionnelle plutôt que `display:none` sur la règle existante — écarté, inutile puisque la demande porte sur TOUS les callouts de la page (pas de variante à conserver), `display:none` directement sur `::before` est plus simple et cascade correctement sur `.callout--warn` sans dupliquer la règle.
**Statut :** révisé par BDR-016 (Pierre a donné une valeur hexadécimale exacte à atteindre). Vérifié à l'époque : `h1`/bouton/fond des cartes en `rgb(244,224,190)` sur l'accueil ; même couleur confirmée sur les titres de CV, RCH et portfolio. Contraste re-vérifié par rasterisation sur le page-hero (CV) : pire cas 6,73-7,37:1 sur 6 angles de teinte. Les 5 blocs `.callout` de la page RCH confirmés avec `::before` à `display:none`. Aucune erreur console, aucun débordement sur les 4 pages testées.

### BDR-016 — `--sable-fonce` fixé à la couleur exacte `#ffefcb`

**Date :** 2026-07-22
**Décision :** `--sable-fonce` passe de `#f4e0be` à `#ffefcb`, la valeur hexadécimale exacte donnée par Pierre. Tous les doublons `rgba()` littéraux mis à jour en même temps (via recherche systématique de l'ancienne valeur, méthode consolidée depuis LRN-010).
**Pourquoi :** Pierre a fourni un code couleur précis à atteindre plutôt qu'une direction relative (« plus clair ») — appliqué tel quel.
**Alternatives envisagées :** Aucune — valeur exacte fournie, pas de marge d'interprétation.
**Statut :** actif. Vérifié : `rgb(255,239,203)` confirmé sur `h1`/fond des cartes de l'accueil et sur le titre du CV. Contraste beige/vert : 7,77:1. Aucune erreur console, aucun débordement.

### BDR-017 — Suppression Photo/Portfolio/À propos, sommaire à 3 cartes égales

**Date :** 2026-07-24
**Décision :** Suppression de `content/pages/photos.md` (page fantôme, non liée, orpheline depuis le retrait du blog), `portfolio.md` et `a-propos.md` (jamais liée nulle part, cohérent avec BDR-001). Toutes les références retirées : carte du sommaire (`index.html`), lien dans `cv.md`, CSS dédié (`.project-card`, `.project-grid`, `.badge--*`, `.tag`). Le sommaire de l'accueil passe de 4 cartes (L/M/M/L, grille 12 colonnes) à 3 cartes égales (`.hub-card--s`, span 4 chacune) — classe déjà présente dans le CSS mais jamais utilisée jusqu'ici. Nettoyage additionnel validé par Pierre : ancien thème mort `theme/` (distinct de `themes/pierre`, jamais référencé par `pelicanconf.py`), dossiers fantômes vides issus d'une commande brace-expansion ratée, et pages `output/` obsolètes (a-propos/portfolio/photos + tout un pan `blog/author/category` généré avant la suppression du blog, que la config actuelle ne régénère plus).
**Pourquoi :** Demande directe de Pierre. La page « Photo » visée était ambiguë (un fichier mort `photos.md` vs. la vraie page vivante `galerie.md`) — clarifié par question explicite avant suppression plutôt que de deviner sur un contenu personnel difficile à restaurer.
**Alternatives envisagées :** Grille asymétrique (une carte « featured » + deux secondaires) pour les 3 cartes restantes — écarté au profit de 3 tiers égaux, plus cohérent avec le minimalisme demandé et réutilisant une classe déjà existante plutôt que d'en inventer une nouvelle.
**Statut :** actif. Vérifié par build Pelican réel (voir LRN-011) : 3 pages générées, aucune référence morte (`grep` sur portfolio/a-propos/photos.html/project-card/badge-- dans tout le thème et le contenu : aucun résultat).

### BDR-018 — Polices auto-hébergées, logos CV en monogrammes locaux

**Date :** 2026-07-24
**Décision :** Fraunces/Inter/IBM Plex Mono ne sont plus chargées depuis `fonts.googleapis.com`/`fonts.gstatic.com` mais servies depuis `themes/pierre/static/fonts/` (mêmes fichiers woff2 que Google sert, récupérés tels quels depuis sa CSS officielle — aucune recompression). Sous-ensemble **latin uniquement** conservé (latin-ext écarté : ~530 Ko économisés, aucun caractère du site n'en a besoin — vérifié par grep de tout `content/`). Nouveau fichier `themes/pierre/static/css/fonts.css`. Séparément, les 5 logos d'entreprise du CV (`cv.md`) ne chargent plus d'image distante (`google.com/s2/favicons`, `cdn.simpleicons.org`) : le monogramme `data-mono` (déjà prévu comme repli CSS, cf. l'entrée « UT » qui n'avait jamais eu d'image) devient le contenu réel pour toutes les entrées, avec un style renforcé (fond `--vert-pale`, 15px/600).
**Pourquoi :** Incohérence relevée par Claude et validée par Pierre : le site auto-héberge déjà GSAP/Anime.js précisément pour ne dépendre d'aucun tiers au runtime (BDR-002/003), mais polices et favicons d'entreprise restaient sur des CDN externes — requêtes bloquantes en plus, contraires à la philosophie déjà actée.
**Alternatives envisagées :** Recompiler les polices variables depuis les sources TTF du dépôt `google/fonts` — écarté, inutilement complexe : les woff2 déjà servis par Google sont déjà optimaux, il suffit de les rapatrier tels quels. Garder les images de logo avec un `onerror` de repli (déjà en place) — écarté, ça n'empêche pas la requête externe initiale à chaque chargement de page.
**Statut :** actif. Vérifié par build réel : `theme/fonts/*.woff2` (404 Ko total) et `theme/css/fonts.css` bien copiés dans la sortie ; `grep` de `fonts.googleapis\|fonts.gstatic\|s2/favicons\|simpleicons` sur tout le thème et le contenu : aucun résultat restant.

### BDR-019 — Galerie alimentée par un lien de partage Immich (au moment du build)

**Date :** 2026-07-24
**Décision :** `content/pages/galerie.md` contient une zone balisée `<!-- GALERIE:START -->`/`<!-- GALERIE:END -->`. Le nouveau script `build_gallery.py` (racine du projet), exécuté avant `pelican content` (localement à la main, et dans `.github/workflows/deploy.yml` via la variable de dépôt `IMMICH_SHARE_URL`), appelle `GET {url}/api/shared-links/me?slug=...` (repli sur `?key=...` si le slug échoue) puis réécrit cette zone avec un `<img>`/`<a>` par photo (vignette `/api/assets/{id}/thumbnail?...`, original en lien). N'écrit sur disque que si le résultat diffère de l'existant (idempotent). Si `IMMICH_SHARE_URL` est absente ou le partage injoignable, la zone n'est pas touchée — le bloc « à venir » d'origine reste affiché.
**Pourquoi :** Une première version (révisée le jour même) faisait ce fetch **côté navigateur** en JS. Testé en direct contre `https://photos.thenaisiepierre.fr/s/testimmich` (premier lien fourni par Pierre) : l'instance Immich ne renvoie **aucun en-tête CORS** (`Access-Control-Allow-Origin` absent même avec un `Origin` envoyé, confirmé par requête directe) — un `fetch()` depuis `thenaisiepierre.fr` aurait été bloqué par tous les navigateurs, silencieusement. Un script exécuté au moment du build fait la même requête HTTP mais côté serveur : CORS est une politique de navigateur, elle ne s'applique pas ici. Les `<img>` générés, eux, chargent bien les vignettes côté visiteur sans aucun souci CORS (un `<img src>` n'y est jamais soumis, contrairement à `fetch()`).
**Alternatives envisagées :** Corriger le CORS côté Immich/Cloudflare plutôt que changer d'architecture — écarté, dépend d'une configuration d'infrastructure hors de ce dépôt, plus fragile qu'un fetch côté build qui fonctionne indépendamment de tout réglage CORS. Lien simple vers la page de partage Immich plutôt qu'une intégration visuelle — écarté, Pierre voulait les photos affichées dans la grille du site (`.gallery-masonry` déjà stylée), pas une redirection hors du design du site. Utiliser une clé API Immich personnelle plutôt que le lien de partage public — écarté, elle serait exposée dans un script exécuté en CI (moins grave qu'exposée côté client, mais toujours un accès complet à la bibliothèque à protéger, alors que le partage est prévu par Immich pour un accès public en lecture seule et révocable indépendamment du compte).
**Statut :** révisé (architecture changée le jour même, cf. ci-dessus) puis actif et **testé avec de vraies données** : script exécuté en réel contre le lien fourni par Pierre, 2 photos récupérées et affichées (vérifié par téléchargement direct de la vignette — image WebP valide, contenu cohérent). Un lien avec mot de passe n'est pas géré (signalé en commentaire dans `galerie.md` et le README) — hors périmètre pour l'instant, pas demandé.

### BDR-020 — `SITE_ABSOLUTE_URL`, distincte de `SITEURL`

**Date :** 2026-07-24
**Décision :** Nouvelle variable `SITE_ABSOLUTE_URL = 'https://thenaisiepierre.fr'` dans `pelicanconf.py`, utilisée pour le lien canonical et toutes les balises Open Graph/Twitter Card (`og:url`, `og:image`, `twitter:image`) dans `base.html`/`page.html` — à la place de `SITEURL`.
**Pourquoi :** Bug trouvé en vérifiant par un vrai build Pelican (pas en relisant le template) : avec `RELATIVE_URLS = True` (actif dans `pelicanconf.py`, seule config réellement utilisée par `deploy.yml`), Pelican réécrit `SITEURL` en chemin relatif (`..`, `.`) pour chaque page — documenté dans son propre code (`pelican/writers.py`, `_get_localcontext`). Une première version utilisant `{{ SITEURL }}` produisait donc des balises `og:url`/`og:image`/canonical relatives (`../pages/cv.html`), invalides pour ces usages qui exigent une URL absolue.
**Alternatives envisagées :** Passer `RELATIVE_URLS = False` globalement — écarté, changerait tous les liens internes du site (portée bien plus large que le seul besoin des balises sociales) et n'est pas ce qui a été demandé. Utiliser directement la chaîne littérale `https://thenaisiepierre.fr` dans les templates — écarté, duplique l'information déjà présente dans `SITEURL` et casserait si le domaine changeait un jour.
**Statut :** actif. Vérifié par build réel : `canonical`/`og:url`/`og:image`/`twitter:image` rendus en URL absolue sur l'accueil et les pages intérieures ; `SITEURL` (favicon, CSS, JS, polices) reste correctement relatif comme le veut `RELATIVE_URLS = True`.

### BDR-021 — `build_gallery.py` : ordre slug/key, User-Agent anti-WAF

**Date :** 2026-07-24
**Décision :** Le script essaie l'identifiant de l'URL de partage comme `slug` d'abord, puis comme `key` en repli (les deux sont des paramètres valides de l'API Immich pour la même requête). Toutes les requêtes HTTP du script envoient un `User-Agent` de navigateur classique (Chrome/Windows), au lieu du défaut `Python-urllib/x.y`.
**Pourquoi :** Testé en direct : le lien fourni par Pierre (`.../s/testimmich`) est un slug personnalisé, pas la clé brute — `?key=testimmich` répond `401 Invalid share key`, `?slug=testimmich` répond `200`. Séparément, la même requête sans `User-Agent` de navigateur échoue en `403 Forbidden` (Cloudflare, devant l'instance Immich, bloque les User-Agent non-navigateur) — rien à voir avec les identifiants, juste une heuristique anti-bot à satisfaire pour un contenu déjà public.
**Alternatives envisagées :** Demander à Pierre de toujours utiliser des clés brutes plutôt que des slugs personnalisés — écarté, contraint inutilement sa façon d'utiliser Immich pour une contrainte du script, pas de l'outil. Désactiver la protection Cloudflare pour l'API — écarté, hors périmètre (infrastructure de Pierre) et pas nécessaire, un simple en-tête suffit.
**Statut :** actif. Vérifié en conditions réelles contre `https://photos.thenaisiepierre.fr/s/testimmich` : 2 photos récupérées et rendues, vignette téléchargée et confirmée être une image valide.

### BDR-022 — `dangerous-clean-slate: true` sur le déploiement FTP

**Date :** 2026-07-24
**Décision :** `dangerous-clean-slate` passe de `false` à `true` dans `.github/workflows/deploy.yml` (action `SamKirkland/FTP-Deploy-Action`). Le dossier distant `www/` sur OVH est désormais entièrement synchronisé sur `output/` à chaque déploiement : tout fichier présent côté serveur mais absent du build local est supprimé.
**Pourquoi :** Pierre a demandé de traiter les pages fantômes possiblement encore en ligne (`blog/`, `author/`, `category/`, `archives.html`, `authors.html`, `categories.html`, `tags.html` — générées par une configuration antérieure à la suppression du blog, jamais nettoyées côté serveur car ce réglage était désactivé jusqu'ici et qu'aucun déploiement n'avait fait le ménage). Aucun accès direct à distance disponible pour vérifier ou nettoyer sélectivement le contenu déjà en place.
**Alternatives envisagées :** Étape de nettoyage ciblée sur les seuls chemins fantômes connus, réglage global laissé désactivé — écarté : plus prudent en théorie (n'affecte que les chemins ciblés) mais impossible à valider sans accès direct depuis cette session, et fragile à écrire à l'aveugle. Le réglage global est une option standard, documentée par l'action elle-même, immédiatement vérifiable au prochain déploiement.
**Statut :** actif, **risque résiduel à surveiller** : ce réglage supprime tout ce qui existe côté serveur et n'est pas produit par ce build — si un autre contenu a été placé manuellement sur ce même chemin (fichier de vérification de domaine, page manuelle, autre usage), ce déploiement l'effacera. Non vérifiable depuis cette session ; à confirmer par Pierre avant le prochain déploiement en cas de doute.

### BDR-023 — Contraste du titre d'accueil corrigé, `.nav-logo`/`.footer-logo` cliquables

**Date :** 2026-07-24
**Décision :** `.hero-immersif h1 em` (le mot en italique du titre d'accueil) passe de `#17241a` (vert forêt plus sombre que le fond, ~1,8:1) à `var(--vert-pale)` (vert clair déjà défini dans `:root`, ~7,6:1 sur `--vert`) — la relation « plus sombre que le fond » actée en BDR-012 est inversée en « plus clair », seule direction mathématiquement capable d'atteindre 3:1 sur un fond aussi sombre (calcul : même en noir pur, le texte plafonnait à ~2,4:1). Séparément, `.nav-logo` et `.footer-logo` (`~/ thenaisiepierre` en en-tête, `Pierre Thenaisie` en pied de page), jusqu'ici des `<span>` inertes sur chaque page, deviennent des `<a href="{{ SITEURL }}/">` avec un survol discret (`opacity: 0.75`).
**Pourquoi :** Audit `/impeccable critique` (voir `.impeccable/critique/`) : le contraste du titre échouait le seuil WCAG même pour du texte large, et aucune page intérieure n'offrait de chemin de retour vers l'accueil une fois passé l'en-tête — l'absence de nav globale (BDR-001) est un choix assumé, mais l'absence de tout lien retour, y compris sur le nom du site lui-même, en était un effet de bord non voulu.
**Alternatives envisagées :** Garder la teinte sombre et ajouter un contour/ombre portée pour la lisibilité — écarté au profit d'une couleur claire directement conforme, plus simple et cohérente avec le token `--vert-pale` déjà existant (pas de nouvelle variable). Ne lier que `.nav-logo` (en haut de page) — écarté : sur `maladie.html` (633 lignes), un lecteur qui a fini sa lecture est en bas de page, pas en haut ; lier aussi `.footer-logo` donne une sortie à l'endroit où elle sert réellement.
**Statut :** actif. Vérifié par build Pelican réel : `rgb(234,240,226)` sur le mot en italique de l'accueil ; `<a class="nav-logo" href="../">`/`<a class="footer-logo" href="../">` corrects sur les 3 pages intérieures (`./` sur l'accueil lui-même).

### BDR-024 — Retrait de GSAP et Anime.js

**Date :** 2026-07-24
**Décision :** `theme/js/vendor/gsap.min.js` (72,9 Ko) et `theme/js/vendor/anime.min.js` (118 Ko) sont supprimés, ainsi que leurs balises `<script>` dans `base.html`. `ethereal.js` perd sa branche `if (window.gsap)` : la boucle `requestAnimationFrame` maison (qui existait déjà comme repli, calculée sur le temps écoulé réel donc insensible aux frames sautées) devient le seul chemin. `animations.js` est réécrit : l'entrée en fondu du hero et la cascade des logos du CV (`staggerLogos`) sont supprimées sans remplacement JS — la première convertie en `@keyframes` CSS échelonné par `:nth-child`, la seconde simplement retirée car `.cv-entry` (son conteneur) est déjà dans la liste `.reveal` de `base.html`, qui anime donc déjà le chip avec lui. Les compteurs numériques (`countUp`) sont retirés (voir BDR-027). Seul le tracé SVG des schémas de la page RCH est reconverti en vanilla (`stroke-dasharray`/`stroke-dashoffset`, ~25 lignes), seul effet qui n'avait pas déjà d'équivalent dans le repo.
**Pourquoi :** Audit `/impeccable critique` : les deux libs (169 Ko) pesaient à elles seules plus que tout le CSS + JS maison réunis, sur un site dont la philosophie assumée (BDR-003) est zéro dépendance runtime superflue, hébergé en mutualisé OVH. `ethereal.js` avait déjà un repli vanilla fonctionnel (écrit lors du calibrage de BDR-005/LRN-006) ; `style.css` avait déjà le pattern de cascade CSS que Anime.js recréait en JS pour le hero.
**Alternatives envisagées :** Garder Anime.js pour le seul tracé SVG (l'usage le plus légitime) — écarté : 118 Ko pour un seul effet, alors qu'un `getTotalLength()`/`stroke-dashoffset` fait la même chose en quelques lignes. Porter `staggerLogos` en CSS dédié plutôt que le supprimer — écarté après vérification que `.cv-entry` anime déjà son contenu (dont le logo) au scroll : un second système d'animation sur le même élément aurait été une duplication, pas une amélioration.
**Statut :** actif. Vérifié par build Pelican réel + `node --check` sur les deux fichiers JS : aucune référence résiduelle à `gsap`/`anime` dans le thème ou les gabarits, poids JS servi 220 Ko → 12 Ko (−95 %). `node .../detect.mjs` réexécuté après coup : aucune régression.

### BDR-025 — Typographie Petrona/Karla remplace Fraunces/Inter

**Date :** 2026-07-24
**Décision :** `--f-disp` (titres) passe de `'Fraunces'` à `'Petrona'`, `--f-body` (corps) de `'Inter'` à `'Karla'`. `--f-mono` (IBM Plex Mono, étiquettes) inchangé. Fichiers récupérés depuis la CSS officielle Google Fonts, même méthode qu'en BDR-018 (sous-ensemble latin, `rsvg-convert`/`fc-list` non nécessaires ici sauf pour l'image Open Graph, voir plus bas) : Petrona en variable 100-900 normal+italique (2 fichiers, comme Fraunces avant elle), Karla seulement en 400 normal+italique (2 fichiers) — poids réellement utilisés seulement, contre 4 graisses chargées pour Inter dont plusieurs n'étaient jamais appliquées à un élément du site (vérifié par grep de tous les `font-weight` sur des sélecteurs `var(--f-body)`). Les dernières références littérales à `'Fraunces'`/`'Inter'` dans les styles internes des schémas SVG (`.svg-h`, `.svg-sub`, `.svg-nt`, `.svg-nsub`) sont passées aux variables CSS, cohérence avec le reste du fichier. `og-image.svg` mis à jour vers les nouvelles polices ; `og-image.png` (pré-rendu) **non régénéré avec certitude** — voir BLK-004.
**Pourquoi :** Audit `/impeccable critique` : le détecteur de design signale Fraunces+Inter (règle `overused-font`, 10 occurrences) comme un duo très répandu dans les interfaces générées par IA — à contre-emploi direct de l'objectif de Pierre (« quelque chose de classe »). Confirmé par Pierre : explorer un nouveau duo plutôt que garder/affiner l'existant.
**Alternatives envisagées :** Affiner Fraunces/Inter par le traitement (tailles, graisses) sans changer les polices — proposé comme option la plus prudente, écarté par Pierre au profit d'un vrai changement. Polices variables complètes (toutes graisses) pour Karla comme pour Inter avant elle — écarté, aucun usage de graisse autre que 400 (normal/italique) trouvé sur `var(--f-body)` dans tout `style.css`.
**Statut :** actif. Vérifié par build Pelican réel : `grep` de `Fraunces`/`'Inter'`/`"Inter"` sur tout le thème généré : aucune occurrence active restante (seuls des commentaires historiques). `node .../detect.mjs` : la règle `overused-font` (10 occurrences avant) a disparu, sans régression ailleurs. Poids polices 404 Ko → 156 Ko (−61 %).

### BDR-026 — Sommaire (TOC) ajouté sur `cv.html`

**Date :** 2026-07-24
**Décision :** `cv.md` reçoit un `<nav class="toc">` identique au composant déjà utilisé sur `maladie.html` (même CSS, aucune nouvelle règle), avec 6 entrées : Expérience, Formation, Compétences, Le fil rouge, Certifications, Me contacter. Les 4 premières ancrent sur l'`id` du `.cv-block` englobant (`#experience`, `#formation`, `#competences`, `#fil-rouge`) plutôt que sur le `.section-label` (qui est un `<div>`, pas un titre) ; « Me contacter » ancre directement sur son `<h2 id="contact">` existant, qui hérite déjà de `.page-content h2[id] { scroll-margin-top: 24px }`. Règle ajoutée pour les 5 autres : `.cv-block[id] { scroll-margin-top: 24px }`, pour la même marge de respiration au clic.
**Pourquoi :** Audit `/impeccable critique` : `cv.html` (420 lignes, structuré en plusieurs blocs) n'avait aucun moyen de sauter directement à une section, contrairement à `maladie.html` qui a le même besoin (page longue) et déjà le composant. Un recruteur, lecteur le plus pressé du site, n'avait aucun moyen d'atteindre directement « compétences » ou le contact.
**Alternatives envisagées :** Ancrer sur les `.section-label` eux-mêmes plutôt que sur `.cv-block` — écarté, ce sont des `<div>` non focusables sémantiquement neutres ; ancrer sur le conteneur de section entier est plus robuste et cohérent avec la manière dont `maladie.html` ancre sur ses vrais `<h2>`.
**Statut :** actif. Vérifié par build Pelican réel : les 6 ancres (`#experience`, `#formation`, `#competences`, `#fil-rouge`, `#certifications`, `#contact`) présentes exactement une fois chacune dans `cv.html` généré, TOC rendu identique en structure à celui de `maladie.html`.

### BDR-027 — Compteurs animés retirés, chiffres RCH statiques

**Date :** 2026-07-24
**Décision :** Les attributs `data-count-to`/`data-suffix` sur les deux `.stat-n` de `maladie.md` (212 700 personnes ; 170+ gènes) sont retirés ; le texte affiché (déjà présent comme valeur finale, Anime.js ne faisait que l'animer par-dessus) reste tel quel, maintenant purement statique.
**Pourquoi :** Audit `/impeccable critique` : un compteur qui défile de 0 à sa valeur est un code visuel de métrique de croissance SaaS, décalé sur un chiffre de santé publique — risque de lire comme une trivialisation en effet marketing d'un chiffre à enjeu réel. Effet de bord direct du retrait d'Anime.js (BDR-024) : `countUp()` n'existait que pour ce seul usage.
**Alternatives envisagées :** Réécrire `countUp` en vanilla pour garder l'animation — écarté, l'animation elle-même était le problème signalé, pas sa seule implémentation via Anime.js.
**Statut :** actif. Vérifié par build Pelican réel : `212 700` et `170+` affichés tels quels dans `maladie.html`, aucun attribut `data-count-to` résiduel.

### BDR-028 — Densité des tirets cadratins réduite (prose uniquement)

**Date :** 2026-07-24
**Décision :** Sur `cv.md` et `maladie.md`, chaque tiret cadratin (—) situé dans une phrase de prose narrative est remplacé par une ponctuation variée selon son rôle réel (virgule pour une apposition légère, deux-points pour une explication/liste qui suit, parenthèses pour une paire de tirets qui encadrait déjà une incise, point pour une rupture forte) — 11 remplacements sur `cv.md`, 27 sur `maladie.md`. Les tirets **structurels** sont laissés intacts : plages de dates (`Décembre 2025 — aujourd'hui`), paires employeur — lieu (`PwC — Neuilly-sur-Seine`), titres de diplôme (`Diplôme d'ingénieur — spécialité Data & IA`) et titres de citations bibliographiques (`INSERM — Dossier «...»`) — convention typographique standard, pas un tic d'écriture. La liste de langues du CV (`Français — natif`, etc.) est convertie en deux-points (`Français : natif`), plus proche d'un vrai couple étiquette/valeur.
**Pourquoi :** Audit `/impeccable critique` : le détecteur signale `em-dash-overuse` (règle avancée mais réelle après vérification manuelle — échantillon d'incises authentiques, pas un faux positif) : 30 tirets sur `cv.html`, 43 sur `maladie.html`, un signal de cadence typique de texte généré par IA. Confirmé par Pierre à traiter dans la même passe.
**Alternatives envisagées :** Remplacer systématiquement, y compris les tirets structurels (dates, lieux, citations) — écarté : ces usages sont un standard universel de mise en forme (CV, bibliographie), pas la source du signal détecté, et les remplacer aurait dégradé la lisibilité sans réduire le vrai problème (la cadence de la prose). Ne rien changer sous prétexte que le contenu de Pierre est déjà jugé très spécifique/personnel (voir verdict de spécificité de la critique) — écarté : la spécificité du contenu et la cadence de ponctuation sont deux axes indépendants, le premier n'excuse pas le second.
**Statut :** actif. Vérifié par build Pelican réel + `node .../detect.mjs` réexécuté : la règle `em-dash-overuse` (2 occurrences avant, sur les 2 pages) a disparu, `cv.html` conserve 13 tirets (tous structurels), `maladie.html` en conserve 7 (tous des titres de citation) — sous le plancher de 8 que la règle exige pour se déclencher.

### BDR-029 — Vrais logos entreprises/écoles restaurés sur le CV

**Date :** 2026-07-24
**Décision :** Les 5 entrées du CV qui avaient un logo avant BDR-018 (PwC, Accenture, ENGIE, SUEZ, EM Normandie, ESILV — 6 au total) reçoivent un `<img>` réel dans leur `.logo-chip`, récupéré une seule fois depuis le site officiel de chaque organisation (favicon/apple-touch-icon en PNG, sauf Accenture converti depuis son `favicon.ico` faute d'une variante PNG carrée disponible) et stocké en local dans `themes/pierre/static/images/logos/`. Le monogramme CSS (`::before`, `content: attr(data-mono)`) reste le repli pour Université de Tours, qui n'a jamais eu de logo (précédent déjà établi avant BDR-018). Nouvelle règle CSS : `.logo-chip img { object-fit: cover }` + `.logo-chip:has(img)::before { content: none }` pour ne jamais superposer les deux.
**Pourquoi :** Demande explicite de Pierre de revenir sur ce point précis de BDR-018 — il veut les vrais logos visibles, pas les monogrammes.
**Alternatives envisagées :** Recharger les logos en direct depuis un service de favicons tiers (comme avant BDR-018) — écarté, c'est exactement la dépendance runtime que BDR-018 avait supprimée pour une bonne raison (requête bloquante à chaque chargement de page) ; l'auto-hébergement obtient le même résultat visuel sans ce coût, en suivant le même principe déjà appliqué aux polices (BDR-018/025). Utiliser un wordmark complet pour Accenture (trouvé sur Wikimedia Commons) plutôt que son symbole seul — écarté après aperçu visuel : le texte « accenture » est illisible une fois rogné dans un cercle de 58px, contrairement à un pictogramme dédié comme les 5 autres logos.
**Statut :** actif. Vérifié par build Pelican réel : les 6 fichiers copiés dans `output/theme/images/logos/`, les 5 `<img>` avec `src`/`alt` corrects dans `cv.html`, Université de Tours toujours en monogramme. Un `broken-image` signalé par le détecteur après coup s'est révélé être un faux positif : le texte `<img>` figurait littéralement dans un commentaire CSS explicatif (pas un vrai tag), corrigé en reformulant le commentaire — plus aucun finding après.

### BDR-030 — Refonte de l'accueil : hero décluttéré, sommaire éditorial, geste de scroll

**Date :** 2026-07-24
**Décision :** Après une maquette (artifact) proposée et validée par Pierre (« totalement ma DA »), l'accueil réel change sur trois points :
1. **Hero** : le fond passe de deux calques `.ethereal` (filtre SVG + grain) + `.mesh-veil` (voile de contraste) en `position:fixed`, à un seul calque `.hero-wash` — des dégradés radiaux CSS purs qui dérivent lentement (`background-position` animé), en `position:absolute` dans `.hero-immersif` (plus fixe pleine page). Mêmes teintes déjà en place (`--vert`, `--vert-clair`, `--vert-pale`), aucune nouvelle couleur.
2. **Sommaire** : les 3 `.hub-card` (cartes en verre dépoli, inclinaison 3D et spotlight au curseur, JS dédié dans `base.html`) sont remplacées par un `.index-list` de 3 `.index-row` — liste éditoriale sobre sur fond beige opaque, chiffres 01/02/03 en très grand (Petrona, jusqu'à 88px, contour seul au repos) qui s'encrent en couleur au survol, ligne qui se teinte de vert pâle au survol. Le sommaire garde son exclusion de toute animation d'apparition au scroll (BDR-014 inchangé).
3. **Geste marquant** : au lieu du hover 3D sur les cartes, le nouveau geste signature est le hero qui se dissout doucement (fondu + léger retrait + flou) en scrollant vers le sommaire — `animation-timeline: scroll(root)` natif, dans une règle `@supports`, zéro JS, zéro coût de scroll-listener. Repli sans support (Firefox à ce jour) : le hero garde simplement son état normal, jamais bloqué invisible.
Nettoyage en cascade : `.hub-grid`/`.hub-card*`/`.home .hub-card*`/`.home .section*`/`.home .picto`/`.home .deco-layer` (CSS), le JS de spotlight/inclinaison des cartes et la parallaxe `.hero` déjà morte (`base.html`), `_deco.html` retiré du sommaire (reste utilisé sur `page.html`, non touché). Nouvelle variable `--vert-pale-rgb` ajoutée (manquait pour un usage `rgba()` cohérent avec LRN-010).
**Pourquoi :** Demande explicite de Pierre d'aller plus loin sur le design de l'accueil : « sobre et minimaliste » avec « des animations sympas », précisé par trois questions ciblées (garder le vert immersif mais désencombré ; un geste marquant plutôt que des micro-interactions partout ; sommaire ouvert à une autre mise en page). La maquette a ensuite été affinée une fois sur la base de son retour (chiffres plus gros, animation au survol du sommaire), puis validée intégralement.
**Alternatives envisagées :** Garder le filtre SVG ethereal et seulement retirer le grain — écarté après que Pierre a vu et adoré la maquette au dégradé CSS pur, plus légère et répondant directement à l'objectif « sans trop de ressources » de la session ; substitution assumée et signalée explicitement plutôt que faite en silence. Généraliser le remplacement du shader par un dégradé CSS aux autres bandeaux du site (CV, RCH) — écarté, hors périmètre : la demande de Pierre portait spécifiquement sur l'accueil, `ethereal.js` reste utilisé tel quel ailleurs.
**Statut :** actif. Vérifié par build Pelican réel + `node .../detect.mjs` : aucune référence résiduelle à `hub-card`/`mesh-holder`/`mesh-veil` dans le thème généré, braces CSS équilibrées, aucun nouveau finding (toujours les 4 `side-tab` déjà classés faux positifs). Rendu visuel non vérifiable directement (pas d'outil navigateur cette session) — à confirmer par Pierre en conditions réelles, en particulier le geste de scroll (`animation-timeline`, non supporté sur Firefox à ce jour). **Piège révélé après coup, voir BDR-031** : le premier déploiement de cette refonte a semblé ne rien changer pour Pierre — cause réelle, `style.css` en cache navigateur (voir BDR-031), pas un bug de cette décision.

### BDR-031 — Casse-cache `?v=<sha-git>` sur les assets statiques

**Date :** 2026-07-24
**Décision :** Nouvelle variable `ASSET_VERSION` dans `pelicanconf.py`, calculée via `git rev-parse --short HEAD` (repli sur un horodatage si git est indisponible). Ajoutée en query string (`?v={{ ASSET_VERSION }}`) sur les liens `fonts.css`, `style.css`, `ethereal.js`, `animations.js` dans `base.html`. Les deux `<link rel="preload">` de polices restent sans version (leur nom de fichier change déjà à chaque changement réel de police, ex. BDR-025 : `fraunces-*` → `petrona-*` — un vrai renommage de fichier casse le cache par construction, contrairement à `style.css` qui garde toujours le même nom).
**Pourquoi :** Après avoir poussé et déployé la refonte de l'accueil (BDR-030), Pierre a vu en ligne le nouveau `index.html` (nouvelles classes `.index-row` etc., nouvelle copie des flèches) mais avec **l'ancien `style.css` encore en cache navigateur** — aucune règle `.index-*` ne s'appliquait, rendu en polices système par défaut. Diagnostic confirmé en vérifiant que le CSS déployé était structurellement valide (comptage rigoureux accolades/commentaires en ignorant les chaînes, aucune anomalie) : la cause n'était pas un bug de code mais l'absence de tout casse-cache sur des fichiers statiques nommés de façon stable, que le navigateur peut continuer à servir depuis son cache après un déploiement même si le contenu serveur a changé.
**Alternatives envisagées :** Renommer `style.css`/`fonts.css` à chaque changement (façon `style.abc123.css`) — écarté, demande de réécrire les références dans plusieurs fichiers et de nettoyer les anciennes versions côté serveur (le déploiement FTP `dangerous-clean-slate`, BDR-022, s'en chargerait, mais ajoute de la complexité de nommage pour un gain identique à une simple query string). Demander systématiquement à Pierre de vider son cache après chaque déploiement — écarté, ce n'est pas une charge à faire porter à l'utilisateur final à chaque changement, le site doit se mettre à jour tout seul.
**Statut :** actif. Vérifié : build réel confirme `?v=b5dc2bf` (SHA du commit courant) sur les 4 liens concernés dans `index.html` généré ; `git rev-parse --short HEAD` fonctionne aussi dans le contexte de `.github/workflows/deploy.yml` (checkout standard `actions/checkout@v4`, `.git` présent). **Ne corrige pas le déploiement déjà en ligne** : Pierre doit encore faire un rechargement forcé (Ctrl+Maj+R) ou ouvrir en navigation privée pour voir la refonte de l'accueil une fois ce correctif déployé — les déploiements suivants n'auront plus ce problème.

### BDR-032 — Retrait des pictogrammes et dégradés décoratifs

**Date :** 2026-07-24
**Décision :** `{% include "_deco.html" %}` retiré de `page.html` (seul template qui l'incluait encore, pour `.page-content` sur CV/RCH/Galerie — déjà retiré de l'accueil en BDR-030). Fichier `_deco.html` supprimé. CSS mort associé retiré : `.picto`/`.picto--1` à `--5`/`.picto-plein`, `.deco-layer::before`/`::after` (les dégradés « lavis »), `.page-content > .deco-layer`/`.section--deco` (`.section--deco` n'était déjà plus utilisée nulle part). Au passage, le `<svg class="svg-sprite">` dans `base.html` (défs `#folio`/`#folio-fin`) est retiré : jamais référencé par aucun `<use>` dans tout le site, mort depuis le début de cette investigation.
**Pourquoi :** Demande explicite de Pierre (capture d'écran de la page Galerie à l'appui) : les petits symboles (cercles concentriques, nœuds reliés, onde, repère) et les halos de couleur en fond de page le gênaient, sur toutes les pages internes.
**Alternatives envisagées :** Retirer seulement l'inclusion sur la page Galerie visée par la capture — écarté, Pierre a explicitement demandé « il y en a sur toutes les pages, supprime-les de partout » ; `_deco.html` n'étant inclus que via `page.html`, un seul point de retrait suffisait à couvrir CV/RCH/Galerie en une fois.
**Statut :** actif. Vérifié par build Pelican réel : `grep` de `deco-layer\|picto\|_deco` sur les 4 pages générées, aucune occurrence restante.

### BDR-033 — `.footer-logo` vers mailto, `.nav-logo` redevient inerte

**Date :** 2026-07-24
**Décision :** `.footer-logo` (« Pierre Thenaisie » en pied de page) passe de `href="{{ SITEURL }}/"` à `href="mailto:contact@thenaisiepierre.fr"`. `.nav-logo` (« ~/ thenaisiepierre » en en-tête) redevient un `<span>` inerte, comme avant BDR-023. CSS de survol (`opacity: 0.75` au survol) retiré pour `.nav-logo`, conservé pour `.footer-logo` (toujours un lien, juste vers une autre destination).
**Pourquoi :** Pierre a explicitement corrigé BDR-023 : cliquer sur son nom ne doit **pas** ramener vers une page qui « ne devrait pas permettre cet accès » — le principe de cloisonnement (BDR-001, un lien partagé ne doit pas exposer le reste du site) prime sur la commodité de navigation que BDR-023 avait ajoutée pour corriger le finding P1 du critique. Pierre a demandé un mailto pour `.footer-logo` spécifiquement ; `.nav-logo` a été remis inerte par cohérence avec le même principe, sans demande séparée — signalé explicitement plutôt que supposé.
**Alternatives envisagées :** Faire de `.nav-logo` un second lien mailto — écarté, redondant avec `.footer-logo` qui remplit déjà ce rôle, et le nav-logo n'a jamais été nommément visé par la demande de Pierre.
**Statut :** actif. Vérifié par build Pelican réel : `<a class="footer-logo" href="mailto:contact@thenaisiepierre.fr">` sur les 4 pages ; `<span class="nav-logo">` (plus de `<a>`) sur les 4 pages.

### BDR-034 — Tracé des schémas RCH réécrit en CSS pur (`pathLength`)

**Date :** 2026-07-24
**Décision :** Chaque tracé animé des 3 schémas de la page RCH (`.svg-colon`, `.svg-inflamme`, `.svg-courbe`, `.svg-lien` hors `.svg-lien--boucle`) reçoit l'attribut SVG natif `pathLength="1"` dans `maladie.md`, qui rend `stroke-dasharray`/`stroke-dashoffset` indépendants de la longueur géométrique réelle du tracé. L'animation est désormais entièrement déclarative en CSS (`.figure.draw` pose l'état masqué, `.figure.draw.in` déclenche la transition), plus aucun calcul `getTotalLength()` ni manipulation de style inline en JS. `animations.js` n'a plus qu'un rôle : ajouter `.draw` puis `.in` sur `.figure` via IntersectionObserver — l'état masqué n'est **jamais** posé en CSS sur le sélecteur nu, seulement sous `.figure.draw`, pour que l'absence de JS laisse les tracés pleinement visibles par défaut (même principe que `.reveal`/`.in` dans `base.html`). Par ailleurs, le premier schéma (comparaison RCH/Crohn) avait son texte de description (`.svg-sub`) qui débordait du `viewBox` côté Crohn (chaîne de 69 caractères sur ~320px de large disponible) : `viewBox` élargi de 700 à 780, et les deux légendes scindées en deux lignes chacune.
**Pourquoi :** Deuxième signalement de Pierre sur ces mêmes animations (la première hardening tentative, en modifiant la technique JS avec reflow forcé, n'a apparemment pas suffi ou n'était pas encore déployée/visible). Plutôt que de re-durcir la même approche JS fragile une troisième fois sans pouvoir tester en navigateur, changement d'architecture vers une technique où l'état de départ est posé une fois pour toutes en CSS statique — élimine la classe entière de bugs de timing (reflow forcé, double rAF, `getTotalLength()` qui échoue) au lieu d'empiler des filets de sécurité. Le débordement de texte a été repéré en marge, en calculant la largeur de caractères disponible par rapport à la longueur des chaînes concernées.
**Alternatives envisagées :** Continuer à durcir la technique JS (reflow forcé + filet `setTimeout`, déjà tentée une fois) — écarté, deuxième récidive du même symptôme signalée par Pierre, signe que le problème est probablement structurel (la technique elle-même) plutôt qu'un détail d'implémentation à peaufiner encore. Réduire seulement la taille de police de `.svg-sub` sans agrandir le `viewBox` ni scinder le texte — écarté, une seule mitigation reposait sur une estimation de largeur de caractères non vérifiable sans navigateur ; empiler plusieurs mitigations indépendantes (scinder + élargir) donne une marge de sécurité même si l'estimation exacte est fausse.
**Statut :** actif. Vérifié par build Pelican réel : 13 attributs `pathLength="1"` présents (2 `svg-colon` + 5 `svg-inflamme` + 1 `svg-courbe` + 5 `svg-lien`), `.svg-lien--boucle` correctement exclu. `node --check` sur `animations.js` : syntaxe valide. **Rendu visuel non vérifiable** (pas d'outil navigateur cette session) — c'est la limite structurelle qui a motivé le changement d'approche vers quelque chose de plus intrinsèquement fiable, mais reste à confirmer par Pierre.

### BDR-035 — Accueil : bande blanche et étiquette « sommaire » retirées

**Date :** 2026-07-24
**Décision :** `.footer` a une règle `margin-top: 110px` globale (pensée pour les pages CV/RCH/Galerie, où le pied de page suit un contenu de page classique) ; sur l'accueil, cette marge laissait apparaître une bande de fond blanc (`body`) entre `.index-section` (déjà opaque, déjà généreusement paddée) et `.footer`. Nouvelle règle `.home .footer { margin-top: 0; }`. Par ailleurs, `<p class="index-eyebrow">sommaire</p>` et son CSS (`.index-eyebrow`, avec le trait de séparation `::after`) sont entièrement retirés du sommaire de l'accueil.
**Pourquoi :** Demande explicite de Pierre. Pour la bande blanche : effet de bord non prévu de la refonte de l'accueil (BDR-030), le nouveau `.index-section` n'existait pas quand `margin-top: 110px` a été réglé à l'origine pour les autres pages. Pour l'étiquette : Pierre juge que le sommaire se comprend sans avoir besoin d'être nommé, et que le trait qui l'accompagnait prenait de la place pour peu d'utilité.
**Alternatives envisagées :** Recolorer la bande en `--sable` plutôt que la supprimer (l'autre option proposée par Pierre) — écarté au profit de la suppression complète, plus simple (une seule valeur à changer plutôt qu'une couleur à faire correspondre) et le résultat visuel est équivalent (`.index-section` et `.footer` se touchent directement, sans bande d'aucune couleur intercalée).
**Statut :** actif. Vérifié par build Pelican réel : `.home .footer` présente dans le CSS généré, `index-eyebrow` absent de `index.html` et de `style.css`.

### BDR-036 — Tracé des schémas RCH repassé en JS impératif

**Date :** 2026-07-24
**Décision :** Retour sur BDR-034 : l'animation du tracé n'est plus pilotée par une transition CSS déclenchée par un changement de classe (`.figure.draw.in`), mais par une boucle `requestAnimationFrame` en JS qui pose `stroke-dashoffset` image par image (easing `easeInOutQuad` codé à la main). `pathLength="1"` (posé en BDR-034) est conservé sur chaque tracé dans `maladie.md` — il simplifie toujours les valeurs de dasharray/dashoffset à une plage 0-1, mais c'est maintenant le JS qui les anime directement, plus aucune transition CSS. Dégradation inchangée dans l'esprit : aucun style n'est jamais posé sur un `<path>` avant que le script ne l'anime réellement (pas de dasharray codé en dur en CSS), donc script absent = tracés pleinement visibles.
**Pourquoi :** Pierre a rapporté l'animation encore cassée après déploiement de BDR-034 (« remets en JS, ça fonctionnait bien en réalité ») — la version CSS pure est donc au moins la deuxième tentative vanilla qui ne fonctionne pas dans son navigateur réel, après la version `getTotalLength()`+reflow forcé (BDR-024, durcie une première fois sans succès confirmé). Sans accès navigateur pour diagnostiquer la cause exacte (aucun outil de capture cette session, comme tout le reste de la session), la décision a été de changer de mécanisme plutôt que de retenter une troisième variante de la même famille de technique (CSS transition déclenchée par une classe) : une boucle `requestAnimationFrame` est le mécanisme le plus proche de ce qu'Anime.js faisait réellement en interne (animer `stroke-dashoffset` directement, frame par frame), sans réintroduire la dépendance elle-même.
**Alternatives envisagées :** Réinstaller Anime.js (`vendor/anime.min.js`, retiré en BDR-024) — écarté : le fichier n'existe plus dans l'arbre de travail (récupérable depuis l'historique git si nécessaire), et reviendrait sur tout l'effort d'allègement validé par Pierre plus tôt dans la même session pour un seul effet ; la boucle rAF vanilla fait la même chose sans les 118 Ko. Retenter la même famille CSS-transition avec un timing différent — écarté, deux échecs consécutifs de mécanismes CSS-transition (dont un avec forced reflow, l'autre purement déclaratif) suggèrent un problème plus profond que le réglage fin, pas la peine d'insister sur la même famille de solution une troisième fois sans pouvoir observer la cause réelle.
**Statut :** actif. Vérifié par build Pelican réel + `node --check` : syntaxe JS valide, aucune régression détecteur. **Rendu visuel toujours non vérifiable depuis cette session** — c'est maintenant la troisième implémentation de cet effet en une seule session sans jamais pouvoir la tester en navigateur réel ; si celle-ci échoue aussi, il faudra sérieusement envisager soit un accès navigateur pour une future session, soit republier Anime.js pour ce seul usage (déjà écarté ici, mais l'option reste ouverte si le vanilla persiste à échouer).

### BDR-037 — Compteurs animés restaurés

**Date :** 2026-07-24
**Décision :** `data-count-to`/`data-suffix` remis sur les deux `.stat-n` numériques de `maladie.md` (212 700 ; 170+), retirés en BDR-027. `animations.js` regroupe maintenant deux effets sous un même helper `observeOnce(selector, onEnter)` (IntersectionObserver générique) : le tracé des schémas (BDR-036) et un nouveau `countUp` en boucle `requestAnimationFrame`, même easing fait main (`easeInOutQuad`) que le tracé, par cohérence.
**Pourquoi :** Pierre a signalé « l'animation ne se fait pas du tout sur les schémas ou même sur les chiffres comme on pouvait l'avoir avant en défilant la page » — regroupant explicitement les deux effets dans la même demande de restauration, alors que le retrait des compteurs (BDR-027) avait pourtant été un choix délibéré et validé (le compteur qui défile jugé trop « marketing » pour un chiffre de santé publique). Pierre revient sur ce choix ; sa préférence prime.
**Alternatives envisagées :** Ne demander confirmation qu'un chiffre à la fois plutôt que restaurer directement — écarté, la demande était explicite et sans ambiguïté (« comme on pouvait l'avoir avant »), pas la peine de faire revalider un choix déjà clairement exprimé.
**Statut :** actif. Vérifié par build Pelican réel : `data-count-to="212700"` et `data-count-to="170"` présents dans `maladie/index.html`, `node --check` sur `animations.js` réussi. Rendu non vérifiable en navigateur, comme le reste de la session.

### BDR-038 — URLs propres (retrait de `/pages/` et `.html`)

**Date :** 2026-07-24
**Décision :** `PAGE_URL`/`PAGE_SAVE_AS` passent de `pages/{slug}.html` à `{slug}/` (Pelican génère alors `{slug}/index.html`) — format standard pour des URLs « propres » sur un hébergement statique classique : un serveur sert automatiquement `index.html` quand un dossier est demandé, aucune configuration serveur (`.htaccess`, réécriture d'URL) n'est nécessaire. Tous les liens internes écrits à la main mis à jour en conséquence : les 3 liens du sommaire dans `index.html` (`/pages/cv.html` → `/cv/`, etc.) et le lien croisé dans `cv.md` vers `maladie.html` → `/maladie/`. Les balises `canonical`/`og:url` (qui utilisent `{{ page.url }}`, calculé par Pelican) se mettent à jour automatiquement, aucune modification nécessaire dans `base.html`/`page.html`.
**Pourquoi :** Demande explicite de Pierre : il ne veut voir ni `/pages` ni `.html` dans la barre d'URL, sur aucune page.
**Alternatives envisagées :** Retirer `.html` sans le `/` final (`{slug}` + réécriture serveur via `.htaccess`) — écarté, demande une fonctionnalité serveur (mod_rewrite Apache) non vérifiable depuis cette session sur l'hébergement OVH mutualisé de Pierre, alors que le format `{slug}/` fonctionne partout sans aucune configuration.
**Statut :** actif. Vérifié par build Pelican réel : structure générée `output/{cv,maladie,galerie}/index.html` (plus de dossier `pages/`), les 4 liens internes mis à jour et vérifiés un par un, `canonical`/`og:url` recalculés correctement (`https://thenaisiepierre.fr/cv/`), chemins vers les assets du thème (CSS/JS/logos) toujours corrects (même profondeur relative qu'avant, `pages/cv.html` et `cv/index.html` étant tous deux à un niveau de profondeur). `dangerous-clean-slate` (BDR-022) nettoiera automatiquement les anciennes URLs `pages/*.html` du serveur OVH au prochain déploiement.

### BDR-039 — Tracé confirmé fonctionnel, halo de lisibilité sur les étiquettes SVG

**Date :** 2026-07-24
**Décision :** Pierre a confirmé que le tracé des schémas RCH fonctionne (BDR-036, boucle rAF) — première confirmation en navigateur réel de toute la session sur cet effet. Question diagnostique posée sur `prefers-reduced-motion` : écartée (« désactivé, normal »). Repéré au passage un vrai défaut de lisibilité, indépendant de l'animation : l'étiquette « inflammation continue » (schéma 1, `.svg-lbl--red`) chevauche le tracé rouge épais (17px) qu'elle est censée décrire, rendant le texte difficile à lire. Plutôt que de recalculer des coordonnées précises sans pouvoir vérifier visuellement le résultat, un halo de la couleur du fond de carte (`var(--sable)`, via `paint-order: stroke fill` + `stroke`) est ajouté à toutes les étiquettes de texte des schémas (`.svg-h`, `.svg-sub`, `.svg-lbl` et ses variantes de couleur) : le texte reste lisible quel que soit ce qu'il y a en dessous, sans dépendre d'un positionnement exact.
**Pourquoi :** Demande explicite de Pierre (« ça empêche la lecture »). Le halo est une correction plus robuste qu'un repositionnement précis des coordonnées SVG, qui aurait nécessité une vérification visuelle impossible cette session — et protège aussi contre des chevauchements similaires non encore repérés sur les deux autres schémas.
**Alternatives envisagées :** Déplacer le texte à une position sans chevauchement — écarté, la place disponible autour du tracé est trop réduite pour un texte de 21 caractères sans risquer de le faire déborder du `viewBox` (même classe de problème que BDR-034) ; le halo règle le problème réel (lisibilité) sans devoir deviner une géométrie non vérifiable.
**Statut :** actif. Vérifié par build Pelican réel + détecteur (aucune régression). Compteurs numériques (BDR-037) signalés par Pierre comme toujours sans effet visible malgré une infrastructure identique au tracé (désormais confirmé fonctionnel) — cause non identifiée avec certitude après relecture attentive du code (aucun bug trouvé), hypothèse retenue : animation de 1,4s trop brève/discrète pour être perçue en scrollant normalement, plutôt qu'un vrai défaut ; à reconfirmer avec Pierre avant de retoucher ce code sans nouveau signal concret.
