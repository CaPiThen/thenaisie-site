---
registre: journal
id_format: date (AAAA-MM-JJ), pas d'ID
description: Journal de session — 3 à 5 lignes maximum par entrée. Pas d'analyse, juste ce qui s'est passé.
champs:
  - date: AAAA-MM-JJ
  - resume: 3 à 5 lignes, factuel
---

# Journal de session — thenaisiepierre.fr

## Index

| Date | Résumé en un mot-clé |
|---|---|
| 2026-07-20 | Nettoyage + refonte design « labo » |
| 2026-07-21 | Contenu RCH/CV + shaders immersifs |
| 2026-07-22 | Mémoire projet + skills GSAP/Anime.js |
| 2026-07-24 | Nettoyage pages + polices/OG/Immich/view-transitions + audit design/correctifs + refonte accueil |

## Entrées

### 2026-07-20

Nettoyage du repo (dossier fantôme, thème mort, `publishconf.py` mort). Refonte visuelle complète : palette forêt/mousse puis fond blanc + vert/marron, typographie très grosse, pas de navigation globale. Installation du skill `ui-ux-pro-max` et tentative d'installation du MCP magic.

### 2026-07-21

Diagnostic complet du MCP magic (cassé en amont, voir BLK-001). Réécriture de la page RCH (schémas SVG, sources vérifiées, alimentation, stress) et du CV (récit par projets, logos d'entreprises). Suppression du blog/journal de vie. Trois itérations de shaders (feuillage → pictogrammes minimalistes, palette marron/gris → 100 % vert, bandeau photo → dégradé maillé). Fusion du sommaire dans le hero immersif façon Velaris, avec correction d'un vrai bug de contraste (verre clair → verre sombre).

### 2026-07-22

Installation des skills GSAP (8 modules officiels) et Anime.js (skill communautaire avec référence API). Mise en place de l'infrastructure mémoire `.claude/memory/` (5 registres) et mise à jour du `CLAUDE.md`. Intégration d'Anime.js v4.5.0 auto-hébergé : tracé des schémas SVG de la page RCH, compteurs numériques sur les chiffres clés, entrée en fondu du hero d'accueil, cascade des logos du CV — décision de rester sur Pelican (BDR-003), la taille sur OVH n'étant pas un facteur limitant.

Remplacement complet des shaders WebGL (`warp.js`/`mesh.js`, pixellisés par sous-échantillonnage) par un fond « ethereal » en filtre SVG natif (`feTurbulence` + `hueRotate` animé), réimplémentation vanilla du composant « Etheral Shadow » de 21st.dev (BDR-004). Calibrage empirique poussé (rasterisation réelle, histogrammes, balayage sur tout le cycle d'animation) pour un mouvement enfin bien visible — deux bugs de contraste trouvés et corrigés en cours de route (espace couleur linearRGB par défaut, et zone de texte protégée par le voile bien plus étroite que la largeur réelle du titre).

Suite le même jour : Pierre trouvait le mouvement encore trop lent malgré une config GSAP déjà correcte — cause trouvée par mesure de fps réelle (~25 fps avec 4 filtres SVG animés en simultané, déclenchant le lag smoothing de GSAP qui ralentit son horloge d'un facteur ~3, LRN-006). Fix : `gsap.ticker.lagSmoothing(0)` + un seul calque animé par zone (le calque accent vert reste figé). Palette repassée en noir/blanc avec touche de vert (BDR-005). Contraste re-vérifié (9,0:1 / 7,3:1 pire cas selon largeur) et présence du vert confirmée par rasterisation. Découverte annexe : l'onglet de prévisualisation ne passe jamais réellement au premier plan, rendant toute mesure de vitesse en temps réel invalide dans cet outil (LRN-007).

Encore plus tard le même jour : Pierre a trouvé ce rythme trop rapide et a demandé de revenir près du rythme d'origine (juste un peu plus vite), et de remplacer la palette noir/blanc/vert par un mélange vert/beige, sans aucun noir (BDR-006). Vitesse repassée à `22s→2,2s` (~15 % plus rapide que le tout premier réglage `26s→2,6s`). Fonds/voiles du noir neutre vers un vert forêt très sombre (`#111f16`). Calque mono recoloré en beige (`--sable-2`), calque accent gardé en vert (`--vert-clair`). En reprofilant le contraste, le voile de l'accueil (`.mesh-veil`) s'est révélé trop juste (4,49:1, proche du seuil AA) — resserré (+0,06-0,08 d'opacité) pour remonter à 5,71:1 partout. Un vieux process Pelican autoreload orphelin bloquait le port 8000 (tué, relancé proprement).

Dernière retouche du jour : Pierre voulait un vert « bien plus clair », en désignant précisément le vert du pied de page (`--vert`, bloc avec son nom) comme référence (BDR-007). Nouvelle couleur accent calculée en HSL pour garder exactement la teinte/saturation de `--vert` en montant seulement la luminosité (91,140,62 → 150,195,155). Contraste revérifié sur les 3 pages : marges quasi identiques à avant (accueil 5,61:1, page-hero 5,85-7,15:1 selon l'angle), toujours confortablement au-dessus du seuil AA.

Tout dernier ajustement du jour : Pierre a finalement demandé de quasiment supprimer le blanc du shader et de repasser à un vert complet, en cherchant de l'harmonie entre toutes les pages/bannières (BDR-008). Calque mono repassé du beige à `--vert-clair` (91,140,62), calque accent gardé en vert clair (150,195,155) — les deux teintes déjà utilisées ailleurs dans la charte (`--vert`, `--vert-clair`). Contraste nettement amélioré par ce changement (le vert est plus sombre que le beige) : accueil 7,93:1, page-hero 8,58-9,51:1 — au-dessus du seuil AAA partout, sans aucun réglage supplémentaire du voile nécessaire.

Et un tout dernier tour : Pierre trouvait quand même l'accueil trop sombre, a demandé de s'inspirer d'autres sites et d'arrêter de trop se soucier du contraste, et de repasser les cartes-liens du sommaire en beige (BDR-009). Fond du hero passé de `#111f16` à `var(--vert)` plein (repris tel quel du pied de page, déjà éprouvé avec du texte blanc). Voile largement allégé (opacités ~divisées par 2). Cartes du sommaire recolorées en beige (`--sable`) avec texte sombre et accents marron, comportement/JS intacts. Changement scopé à la page d'accueil uniquement.

Dernier tour du jour : Pierre a généralisé la règle — tout ce qui repose sur du vert sur l'accueil doit être beige (polices comprises), sauf nuances ponctuelles en vert foncé/noir, et le cadre visible autour des cartes du sommaire doit disparaître car ça ressemblait à « une page dans la page » (BDR-010). Tous les textes directement sur le fond vert (label, titre, sous-titre, bouton « Découvrir », label de section, pictogrammes, lueurs décoratives, indicateur de défilement) recolorés en beige ; seul le mot en italique du titre reste sombre (`--encre`) comme unique nuance, avec un contraste très faible (1,77:1) assumé et signalé à Pierre plutôt que masqué. Bordure des cartes passée en transparent (pas de `border: none`, pour ne pas décaler le padding).

Et une clarification juste après : Pierre voulait un beige plus foncé, et a précisé que le « cadre » qu'il visait était en fait la bande translucide du séparateur après le label « // sommaire », pas la bordure des cartes (BDR-011). Nouvelle variable `--sable-fonce` (#d8c3a0) appliquée à tout le beige de l'accueil ; le séparateur de section passé en transparent. Effet de bord détecté et corrigé au passage : le texte `--mut` des cartes tombait à 2,97:1 sur ce beige plus sombre — remonté à 6,19:1 avec une teinte dédiée.

Trois demandes en une pour finir la journée : (1) `--sable-fonce` réclairci à `#e6d2b0` ; (2) plus de noir/gris pour les nuances de contraste, remplacé par du vert forêt (`var(--vert)` pour le texte des cartes, un vert forêt encore plus sombre que le fond pour le mot en italique du titre, sous peine de le rendre invisible) (BDR-012) ; (3) un vrai effet « bloc qui tourne » entre le titre et le sommaire, inspiré d'une animation façon 21st.dev (BDR-013). Installé GSAP ScrollTrigger (auto-hébergé, même version que gsap.min.js). Hero et sommaire enveloppés dans `.flip-stage`, empilés dans la même cellule de grille et pivotés en 3D en sens inverse pendant un scroll épinglé d'une hauteur d'écran — nécessaire pour que les deux faces soient visibles simultanément (un simple double ScrollTrigger sans conteneur partagé aurait laissé le sommaire hors écran pendant toute la transition). Deux bugs réels trouvés en vérifiant, pas de simples ajustements : `scroll-behavior: smooth` (réglage global existant) cassait le calcul de position de ScrollTrigger (LRN-008) ; le fond ethereal `position:fixed` devait être sorti de `.flip-stage` avant que sa nouvelle `perspective` ne le recale dessus au lieu du viewport (LRN-009, anticipé par un commentaire déjà présent dans le CSS). Vérifié par scroll programmatique : progression 0→0,5→1 cohérente, épinglage puis relâchement corrects, repli mobile propre (<900px : aucune classe, aucun ScrollTrigger actif), pages CV/RCH non affectées.

Revirement immédiat : Pierre a abandonné l'effet de bloc qui tourne, qu'il ne voulait finalement pas — il cherchait un défilement lisse, sans aucune section qui « apparaisse » (BDR-014). Tout retiré proprement : `.flip-stage`/`.flip-face` (gabarit + CSS), `flip-transition.js` et `ScrollTrigger.min.js` supprimés du thème et de `output/`, script retiré de `base.html`. `.mesh-holder` remis à sa place d'origine dans `.hero-immersif`. `scroll-behavior: smooth` restauré (le conflit disparaît avec le plugin). Au passage, `.hub-card` retiré de la liste de révélation au scroll dans `base.html` : les cartes du sommaire sont maintenant toujours pleinement visibles, sans fondu/décalage à l'entrée dans le viewport. Vérifié : shader intact, aucune classe/JS résiduel du flip, `.project-card` du portfolio garde bien son animation de révélation (changement scopé à l'accueil uniquement).

Dernière série de retouches : Pierre voulait les boutons de l'accueil encore plus clairs (proches du blanc), cette même couleur reprise pour les titres de toutes les autres pages, et les barres latérales des blocs de contexte de la page RCH supprimées (BDR-015). `--sable-fonce` réclairci à `#f4e0be`. En vérifiant, découvert que plusieurs `rgba()` littéraux (hero-sub, bouton, picto, lueurs, indicateur de défilement) étaient restés bloqués sur l'ancienne valeur de BDR-011, jamais suivis lors du passage à BDR-012 — corrigé au passage (LRN-010, un vrai bug de désynchronisation, pas une simple retouche). Titres de CV/RCH/portfolio recolorés dans la même teinte. Barre des 5 blocs `.callout` de la page RCH masquée. Contraste revérifié partout : encore meilleur qu'avant (6,7-7,4:1 pire cas sur les pages intérieures, 6,84:1 sur l'accueil), la teinte plus claire portant naturellement plus de contraste sur fond sombre.

Micro-retouche juste avant le premier déploiement : Pierre a donné une valeur hexadécimale exacte (`#ffefcb`) pour les boutons de l'accueil (BDR-016) — appliquée telle quelle à `--sable-fonce` et à tous ses doublons littéraux. Contraste : 7,77:1. Puis premier commit + push de toute cette session (voir `.github/workflows/deploy.yml` : le push sur `main` déclenche le build Pelican et le dépôt FTP vers OVH automatiquement) — rien de tout le travail accumulé (shaders, palette, pages CV/RCH/portfolio/galerie, mémoire projet) n'avait encore été commité jusqu'ici.

### 2026-07-24

Exploration complète de l'arborescence puis suppression des pages Photo (le vrai fichier mort était `photos.md`, orphelin depuis le retrait du blog — clarifié auprès de Pierre plutôt que deviné, la vraie galerie vivante étant `galerie.md`), Portfolio et À propos, avec toutes leurs références (sommaire, `cv.md`, CSS dédié) et le CSS mort associé retirés (BDR-017). Nettoyage bonus validé par Pierre : thème mort `theme/`, dossiers fantômes d'une commande brace-expansion ratée, pages `output/` obsolètes dont tout un pan `blog/author/category` généré avant la suppression du blog.

Retour design formulé (View Transitions, polices/logos auto-hébergés, Open Graph/favicon, variables `--xxx-rgb`), puis Pierre a demandé d'implémenter chacun en profondeur, et a précisé vouloir alimenter la galerie depuis des albums Immich connectés à son NAS. Réalisé : polices Google Fonts entièrement auto-hébergées en sous-ensemble latin (BDR-018, ~400 Ko, fichiers récupérés tels quels depuis la CSS officielle de Google) ; les 5 logos d'entreprise du CV ne chargent plus depuis des services de favicons tiers, remplacés par des monogrammes CSS déjà prévus comme repli ; View Transitions API activée (une seule règle CSS, dégradation invisible) ; toutes les couleurs dupliquées en `rgba()` littéral remplacées par des variables `--xxx-rgb` (fixe la classe de bug de LRN-010 à la racine) ; favicon (SVG + PNG) et image Open Graph entièrement dessinés et rendus avec les vraies polices/couleurs du site (`rsvg-convert`, installé pour l'occasion) ; galerie alimentée par le mécanisme public de partage Immich, contrat d'API vérifié depuis la spec OpenAPI officielle du projet plutôt que deviné (BDR-019), désactivée par défaut tant que Pierre n'a pas fourni son URL/clé de partage.

Tout vérifié par un vrai build Pelican (venv jetable, Pelican installé pour l'occasion) plutôt qu'une simple relecture des gabarits — ce qui a révélé un vrai bug : `SITEURL` est réécrit en chemin relatif par Pelican quand `RELATIVE_URLS=True`, rendant les balises `og:url`/`og:image`/canonical invalides (relatives) si on les construit avec cette variable. Corrigé par l'ajout de `SITE_ABSOLUTE_URL`, un nom que Pelican ne réécrit pas (BDR-020, LRN-011). Friction annexe : `rm -rf output/` bloqué par une entrée fantôme côté cache CIFS (le projet est monté depuis un NAS) — contourné en construisant la vérification dans un répertoire de scratch (BLK-003).

Pierre a fourni un premier vrai lien de partage Immich (`https://photos.thenaisiepierre.fr/s/testimmich`) pour tester. Deux bugs réels trouvés en testant en conditions réelles, pas de simples ajustements : (1) l'instance Immich ne renvoie aucun en-tête CORS — le fetch côté navigateur prévu dans la version précédente aurait échoué silencieusement sur tous les navigateurs (LRN-013) ; (2) même en corrigeant l'architecture, le script échouait encore en `403` à cause du Cloudflare devant l'instance qui bloque les requêtes sans User-Agent de navigateur (LRN-014). Architecture revue en conséquence (BDR-019 révisé) : nouveau script `build_gallery.py`, exécuté au moment du build (localement et dans `deploy.yml` via la variable `IMMICH_SHARE_URL`), qui récupère les photos côté serveur et réécrit une zone balisée de `galerie.md` — plus aucun JS côté client pour la galerie. Testé en réel : 2 photos récupérées, vignette téléchargée et confirmée être une image valide. README mis à jour avec les instructions de configuration.

Avant de committer, remis la page Galerie en « à venir » : le lien fourni était explicitement présenté par Pierre comme un test, pas comme son album définitif — pas de publication de contenu de test sous son nom sans confirmation. Repéré et corrigé au passage un vrai bug de dérive de fins de ligne (CRLF) sur `cv.md`/`maladie.md`, sans rapport avec cette session, qui aurait pollué le commit d'un diff de centaines de lignes sans changement réel. Commit local fait ; le push a échoué (pas d'identifiants GitHub dans cet environnement). Pierre a ensuite demandé de traiter les pages fantômes signalées plus tôt : `dangerous-clean-slate` passé à `true` dans `deploy.yml` (BDR-022) faute d'accès direct pour un nettoyage ciblé plus prudent.

Nouvelle session le même jour : demande de Pierre de revoir le style et les animations du site, avec l'objectif explicite d'aller vers « quelque chose de classe » en restant léger en ressources. Lancement de `/impeccable critique` en dual-agent (Assessment A design, Assessment B détecteur — preuve navigateur indisponible, aucun outil de capture exposé cette session, installation refusée par l'utilisateur). Score 24/32 (75 %, « Bon »). 7 problèmes priorisés (P0 à P3), Pierre a demandé de tous les traiter dans l'ordre, avec exploration d'un nouveau duo typographique plutôt qu'un simple affinage, et rien hors périmètre. Exécutés un par un, chacun vérifié par un vrai build Pelican + réexécution du détecteur (BDR-023 à BDR-028) : contraste du titre d'accueil corrigé et `.nav-logo`/`.footer-logo` rendus cliquables ; GSAP + Anime.js retirés entièrement (169 Ko), deux des trois effets remplacés se sont révélés déjà couverts par du code existant dans le repo, découvert en vérifiant avant d'écrire du nouveau JS (LRN-015) ; typographie Fraunces/Inter (signalée « générique IA » par le détecteur) remplacée par Petrona/Karla auto-hébergées ; TOC ajouté sur `cv.html` sur le modèle de `maladie.html` ; compteurs animés sur les chiffres RCH rendus statiques ; densité de tirets cadratins réduite en triant structurel (dates, lieux, citations — jamais touché) vs prose (LRN-016). Poids total du site généré : 984 Ko → 405 Ko. Un point non résolu : la régénération de l'image Open Graph (`og-image.png`) avec les nouvelles polices n'a pas pu être vérifiée, `rsvg-convert` semblant ignorer le `font-family` demandé dans ce sandbox (BLK-004) — la source SVG est à jour, le PNG pré-rendu ne l'est pas, signalé explicitement à Pierre plutôt que masqué. Deux commits locaux créés pour ce travail, mais le push a échoué : jeton GitHub collé en clair dans le chat, bloqué par le classificateur de sécurité de Claude Code — récidive d'un incident déjà documenté (BLK-005), recommandation de régénérer le jeton transmise à Pierre.

Suite immédiate, même session : Pierre a signalé le tracé SVG des schémas RCH comme apparemment cassé — en réalité jamais retiré, mais la technique vanilla de remplacement (double `requestAnimationFrame`) a été durcie par précaution (reflow forcé par lecture, filet de sécurité `setTimeout`, LRN-017) faute de pouvoir vérifier visuellement. Les logos d'entreprises/écoles du CV ont été restaurés (BDR-029) : récupérés une fois depuis chaque site officiel (favicon/apple-touch-icon, Accenture converti depuis un `.ico`) puis auto-hébergés — jamais rechargés en direct depuis un tiers, même logique que les polices. Enfin, demande d'aller plus loin sur le design de l'accueil : trois questions ciblées ont cadré la direction (vert immersif gardé mais désencombré, un geste marquant au scroll plutôt que des micro-interactions partout, sommaire ouvert à une nouvelle mise en page), une maquette complète a été construite en artifact (polices réelles du site intégrées en base64 pour une fidélité exacte) puis affinée une fois sur retour de Pierre (chiffres du sommaire agrandis, animation d'encrage au survol) avant validation complète (« totalement ma DA »). Implémentée ensuite sur le vrai site (BDR-030) : hero simplifié à un seul calque de dégradé CSS animé (remplace les deux calques ethereal + grain + voile), sommaire transformé en liste éditoriale à grands chiffres, geste de scroll signature en `animation-timeline` natif (zéro JS). Nettoyage en cascade du CSS et JS mort associés (spotlight/inclinaison des cartes, parallaxe `.hero` déjà orpheline). Vérifié par build réel + détecteur, mais **rendu visuel non confirmé en navigateur** (aucun outil de capture cette session) — à valider par Pierre.

Pierre a demandé de committer (deux commits créés), puis tenté de pousser lui-même avec succès — mais a rapporté ne rien voir de neuf en ligne. Diagnostic à distance (aucun accès réseau authentifié pour vérifier soi-même) : CSS structurellement valide (vérifié par un tokenizer maison ignorant chaînes/commentaires), donc très probablement un cache navigateur sur `style.css` (même nom de fichier à chaque déploiement, rien ne forçait un rechargement). Correctif structurel : `ASSET_VERSION` (SHA git court) ajoutée en query string sur CSS/JS (BDR-031), pour que ce problème ne se reproduise plus sur les prochains déploiements. Pierre a ensuite envoyé une capture d'écran réelle du site déployé (page Galerie) accompagnée de 5 demandes : retirer les pictogrammes/dégradés décoratifs de toutes les pages internes (BDR-032, `_deco.html` supprimé) ; faire pointer le nom en pied de page vers un mailto plutôt que l'accueil, corrigeant une partie de BDR-023 qui allait à l'encontre du principe de cloisonnement (BDR-033) ; récupérer les animations des schémas RCH signalées cassées une deuxième fois et corriger un texte de description qui débordait — cette fois, plutôt que redurcir la même technique JS, tracé réécrit entièrement en CSS pur via `pathLength` (BDR-034), avec au passage la découverte d'un vrai risque de régression (état masqué scopé sur un mauvais sélecteur aurait rendu les schémas invisibles sans JS, corrigé avant de committer, LRN-020) ; retirer une bande blanche entre le sommaire et le pied de page de l'accueil, effet de bord de la refonte précédente ; retirer l'étiquette « sommaire » (BDR-035). Tout vérifié par build réel + détecteur (aucune régression), commité. Rendu visuel toujours non confirmable depuis cette session — capture d'écran de Pierre étant la seule preuve visuelle disponible.

Dernier retour de la session : tout fonctionne sauf, encore, l'animation des schémas RCH — la version CSS pure (BDR-034) ne marchait pas non plus dans son navigateur réel. Pierre a explicitement demandé de repasser en JS, précisant que la version JS d'origine « fonctionnait bien en réalité ». Plutôt que de retenter une troisième variante de transition CSS déclenchée par classe (deux échecs consécutifs sur la même famille de technique), changement de mécanisme : boucle `requestAnimationFrame` qui pose `stroke-dashoffset` image par image en JS direct, en gardant `pathLength="1"` (BDR-036) — le mécanisme le plus proche de ce qu'Anime.js faisait réellement en interne, sans réinstaller la dépendance (retirée en BDR-024, effort validé par Pierre plus tôt dans la même session). Signalé explicitement à Pierre : c'est la troisième implémentation de cet effet dans la session sans jamais pouvoir la tester en navigateur réel (LRN-021) — si celle-ci échoue aussi, il faudra soit un accès navigateur pour une prochaine session, soit reconsidérer Anime.js pour ce seul usage.

Pierre a répondu que l'animation ne se déclenche toujours pas du tout — et a précisé, nouvel élément, que les compteurs numériques (retirés délibérément en BDR-027) manquaient aussi « comme on pouvait l'avoir avant en défilant la page ». Compteurs restaurés avec la même technique rAF que le tracé (BDR-037, annule BDR-027). Vu que trois implémentations différentes du tracé ont toutes été rapportées non fonctionnelles, hypothèse posée mais pas encore confirmée : `prefers-reduced-motion` pourrait être actif sur l'appareil de Pierre, ce qui expliquerait uniformément l'absence de tout effet JS (le tout premier test de `animations.js` est un retour anticipé sur cette media query) — question posée à Pierre plutôt que de tenter une quatrième implémentation à l'aveugle. Deuxième demande dans le même message, sans rapport : URLs propres. `PAGE_URL`/`PAGE_SAVE_AS` passés de `pages/{slug}.html` à `{slug}/` (BDR-038), tous les liens internes mis à jour, vérifié que canonical/og:url et les chemins vers les assets du thème restent corrects (même profondeur relative).

Pierre a répondu : `prefers-reduced-motion` désactivé/normal chez lui, donc cause écartée. Bonne nouvelle ensuite : le tracé fonctionne enfin (première confirmation en navigateur réel sur cet effet, après trois tentatives). Deux points restants dans le même message : les compteurs numériques ne semblent toujours pas s'animer (relu attentivement le code, aucun bug trouvé — hypothèse retenue : animation de 1,4s trop brève pour être perçue en scrollant, à reconfirmer avant de retoucher sans nouveau signal) ; et un vrai défaut repéré — l'étiquette « inflammation continue » du premier schéma chevauche le tracé rouge et devient illisible. Corrigé avec un halo de fond (BDR-039) plutôt qu'un repositionnement de coordonnées non vérifiable visuellement, technique appliquée à toutes les étiquettes des 3 schémas par précaution.

Nouvelle demande le lendemain : ajouter une image de fond au hero de l'accueil avec des « tracés saillants », en s'inspirant d'une photo personnelle de sommets alpins. Plutôt qu'un dessin à main levée (première tentative, jugée pas assez fidèle par Pierre : « fais en sorte que ça commence... trop de décalage entre la vraie montagne et le contraste »), la photo a été réellement vectorisée avec `potrace` (deux seuils de contraste sur la même image recadrée à l'identique, pour que les deux calques restent alignés) — BDR-041. Itéré en artifact (fidélité du tracé, estompage du relief à droite trop épars, vitesse de la dérive, fondu synchronisé avec le geste de dissolution au scroll déjà existant) jusqu'à validation complète. Demande suivante, sans rapport : un quadrillage de points sur le fond sable du sommaire — d'abord tenté en dégradé de couleur (annulé sur demande de Pierre, jugé trop abrupt), puis en points CSS statiques (taille ajustée deux fois, 1.5px → 2.5px → 1.9px), puis rendus magnétiques (« qu'ils se rapprochent du curseur ») via un unique `<canvas>` repeint en boucle `requestAnimationFrame`, plutôt que des centaines d'éléments DOM — le suivi de souris reste posé sur `.index-section`, jamais sur le canvas (`pointer-events: none`), pour que les 3 liens du sommaire restent cliquables. Enfin, Pierre a demandé que le lien du nom en haut à gauche (`.nav-logo`) ramène de nouveau vers l'accueil — contredisant directement BDR-033 (qui l'avait rendu inerte pour respecter le cloisonnement des pages partagées). Signalé explicitement le conflit avant d'appliquer (BDR-040) : la demande étant récente et explicite, elle prime. Interrogé ensuite sur la portée du déploiement (juste ce lien, ou toute la maquette validée) : Pierre a choisi de tout implémenter maintenant. L'ensemble (silhouette potrace, quadrillage magnétique, lien nav-logo) porté sur le vrai site — les deux tracés SVG (~65 Ko à eux deux) gardés hors du HTML, dans `data/hero-peaks-{back,front}.path`, chargés par `pelicanconf.py` et exposés en globals Jinja comme `ASSET_VERSION`, plutôt qu'incrustés illisibles dans le template. Vérifié par build Pelican réel (les 4 pages), `node --check` sur le JS, détecteur de design (aucune régression, 4 avertissements pré-existants sans rapport), et un tokenizer CSS maison confirmant la structure. Pas encore commité à ce stade.

Pierre a demandé de committer : fait (commit `bf2b133`), poussé par Pierre lui-même. Juste après, il a signalé ne pas voir toutes les animations en ligne (notamment sur la page RCH) ni les points d'accueil — cette fois, contrairement à tout le reste de la session, une vérification directe a été possible : `curl` nu reçoit un 403 de Cloudflare (le domaine est proxifié, découverte du jour), mais avec un User-Agent de navigateur classique il passe (LRN-022). HTML/CSS/JS comparés octet pour octet entre le site déployé et le code local commité (accueil et page RCH) : strictement identiques, version de cache-busting correcte (`?v=bf2b133`). Déploiement confirmé correct à 100 % ; diagnostic transmis à Pierre : cache navigateur côté client, rafraîchissement forcé recommandé. Confirmé résolu ensuite par Pierre.

Dernière demande de la session : ajouter au moins une photo à la page Galerie, encore en « à venir » (BDR-019). Premier lien de partage Immich fourni pointait en fait vers une vidéo — détecté immédiatement (le script filtre correctement sur `type=="IMAGE"`), signalé à Pierre plutôt que traité comme un bug. Deuxième lien, un album réel (« Mon site », 4 photos) : `build_gallery.py` trouvait 0 photo malgré des métadonnées indiquant 4 assets. Investigation via le code source du front Immich (plutôt qu'un correctif à l'aveugle) : Immich a retiré les assets d'album de `/api/shared-links/me` pour alléger le payload (PR immich-app/immich#17207) ; il faut désormais passer par l'API timeline (`/api/timeline/buckets` puis `/api/timeline/bucket`, format « colonnes »), utilisée par le front lui-même. `build_gallery.py` corrigé en conséquence (BDR-042), les partages `INDIVIDUAL` restant inchangés. Vérifié empiriquement contre l'instance réelle de Pierre : 4 photos retrouvées, vignettes confirmées valides (`image/webp`, 200), et par un vrai build Pelican (page Galerie générée avec les 4 `.photo-item`). Commité (`a823827`) ; Pierre a poussé et ajouté `IMMICH_SHARE_URL` en variable de dépôt lui-même, puis ajouté une 5e photo — confirmé en ligne après un « Re-run all jobs » manuel (le workflow ne se déclenche que sur `push`, pas de `workflow_dispatch`). Cadre retiré autour des photos de la galerie sur demande (BDR-044), commité (`d4b6706`).

Pierre a signalé un avertissement GitHub sur la dépréciation de Node20 : les trois actions du workflow de déploiement tournaient encore dessus (vérifié dans leurs `action.yml`), bumpées vers Node24 (checkout v4→v7, setup-python v5→v7, FTP-Deploy-Action v4.3.5→v4.4.0), notes de version lues pour écarter tout changement cassant (BDR-043).

Demande d'ajouter un mini-menu façon « labo personnel » dans la nav — question de cadrage posée (emplacement), Pierre a répondu vouloir ça dans la barre du haut, mais avant implémentation il a interrompu et redirigé vers une liste de 4 correctifs concrets après avoir demandé une relecture complète du dossier projet. Traités un par un : fond des pastilles CV aligné sur le fond de page, empilement mobile de l'en-tête d'entrée (la pastille passait à côté du texte au lieu d'au-dessus, désalignant le titre du reste du contenu), adresses `.place` éclatées en plusieurs lignes (entreprise / lieu / client), bordures et espace retirés autour du sommaire d'accueil (BDR-046). Entre-temps, un commit antérieur (`db28e0c`, retrouvé dans l'historique sans trace claire de la demande dans ce qui était visible) avait aussi retiré l'étiquette du hero et le bouton « Découvrir » — signalé explicitement à Pierre pour confirmation plutôt que supposé correct (BDR-045).

Pierre a envoyé deux captures d'écran : le trait vertical de la frise CV touchait le texte sur mobile (comparé à `.story-item` plus bas sur la page, correctement décalé) ; et une « coupure de couleur bizarre » sur le bord droit des bandeaux de pages secondaires. Le premier point était un oubli du correctif précédent (`.cv-entry` remis à `padding-left: 22px`, comme `.story-item`). Le second, identifié en lisant le CSS plutôt que supposé être un artefact de bruit du shader : `.shader-scrim` chutait de 0.68 à 0.26 d'opacité sur seulement les 10 derniers % de largeur, trop abrupt pour lire comme un fondu — adouci en deux paliers sans déplacer le point de départ à 90% (contrainte de contraste du titre) (BDR-047). Commité (`8afb9ec`).

Fin de session : mémoire mise à jour (BDR-043 à BDR-047, LRN-023 sur les dégradés confinés à une petite plage, BLK-006 sur un phénomène de stage git non expliqué observé deux fois — fichiers pré-existants non liés marqués comme stagés sans `git add` explicite, contourné à chaque fois par une relecture de `git status` avant tout commit).

Nouvelle session (2026-07-30) : Pierre a signalé un deuxième symptôme sur le bandeau des pages secondaires malgré le voile adouci en dégradé (BDR-047) — capture d'écran demandée pour la première fois du projet, révélant grâce à chromium headless nouvellement installé (LRN-024) que la tache claire venait de la variance naturelle du bruit du shader, pas du dégradé du voile. Corrigé par un voile uniforme et plus opaque (BDR-048), vérifié visuellement sur les 3 pages secondaires + mobile avant livraison — première fois du projet qu'un correctif visuel est confirmé par capture d'écran réelle avant d'être annoncé résolu.

Puis Pierre a annoncé la désactivation de son hébergement OVH et demandé la migration vers GitHub Pages (domaine + DNS Cloudflare conservés) : `deploy.yml` réécrit pour le pipeline officiel GitHub Pages (versions d'actions vérifiées une par une, pas devinées), `CNAME` généré par Pelican via `STATIC_PATHS`/`EXTRA_PATH_METADATA` (BDR-049). Au passage, découvert que le bump Node20→24 du workflow (BDR-043, plus tôt dans la session précédente) n'avait en réalité jamais été commité — corrigé directement dans la réécriture. Pierre a dû rendre le dépôt public pour activer Pages (gratuit) ; question légitime sur les risques (fork, écriture) — répondu après vérification réelle de l'historique complet du dépôt (aucun secret jamais commité, y compris le jeton GitHub de l'incident BLK-005).

Migration mouvementée mais entièrement diagnostiquée à distance, sans jamais avoir accès aux comptes Cloudflare/GitHub de Pierre : (1) DNS mal réparti (1 IP sur l'apex, 3 sur `www`) repéré en lisant la capture d'écran de Pierre en détail ; (2) `git push` de Pierre rejeté par un commit `Create CNAME` généré automatiquement par GitHub Pages, `git pull --rebase` cassé à mi-parcours, résolu par `rebase --abort` puis `merge` (BLK-007) ; (3) certificat TLS bloqué sur `*.github.io` générique malgré un DNS en apparence correct, cause réelle trouvée par `dig` sur plusieurs résolveurs publics — un enregistrement A restait « Proxied » sur Cloudflare (LRN-025) ; diagnostic confirmé et bouclé par `openssl s_client` + `curl` réels (LRN-026). Migration confirmée fonctionnelle en ligne par Pierre puis revérifiée depuis cette session (`200`, `server: GitHub.com`, certificat `CN=thenaisiepierre.fr` valide).

Nouvelle session (2026-07-31) : deux correctifs demandés après capture d'écran. Le titre du hero n'était pas centré sur mobile — cause : une règle `text-align: left` mobile posée dès la refonte d'origine sans justification documentée, sans effet voulu vu que `.hero-immersif__contenu` se réduit à sa largeur de contenu (le bloc entier se recentre visuellement, mais le texte à l'intérieur repart à gauche) ; supprimée, le titre hérite du centrage de `.hero-immersif`. Sur la page RCH, le disclaimer « à lire avant tout » est sorti de l'encart beige (`callout--warn`) vers une nouvelle classe dédiée `callout--intro` (filet gauche marron, label italique, pas de fond) plutôt que de modifier `callout--warn` directement — cette classe est aussi utilisée par un second callout plus bas sur la page que Pierre n'a pas demandé de changer (BDR-050). Vérifié par build Pelican réel + capture d'écran chromium (desktop/mobile, accueil/RCH) : première fois qu'une capture chromium montrait un texte quasi invisible sans bug CSS réel — cause trouvée (l'animation `.reveal` au scroll n'avait pas eu le temps de se déclencher avant la capture), contournée avec `--force-prefers-reduced-motion` (LRN-027).
